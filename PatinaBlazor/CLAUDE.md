# PatinaBlazor Project Notes

## Project Purpose & Context

### What PatinaBlazor Is
A personal web app with modular, independent features. The name "Patina" reflects the core concept — aged, valuable antique items. Features are designed to be self-contained so each area of the app can grow independently without coupling.

### Features
- **Collections / Collectables** — The original and core feature. Allows tracking of antique collections. Still being refined.
- **Chat (IRC Bridge)** — A mIRC-style chat interface that bridges to IRC networks. A Python IRC bot logs events to the app via REST API; admins can monitor live IRC activity in a retro terminal-style UI.
- **RV & Boat Storage** — Phase 1 (admin-only) of a storage rental business module: properties with photos, units with dimensions/pricing, and a Storage Admin dashboard with occupancy/revenue charts. See Working Checkpoint below. Customer self-signup and recurring billing are a planned Phase 2, not yet built.
- **Articles** — a content feature authored by "Article Publisher" or Admin users, with Markdown bodies, per-article audience gating (Public/RegisteredUsers/StorageCustomers), and optional featuring on the Home page. See Working Checkpoint below.
- *(Additional features are expected to be added over time in a modular way)*

### Design Philosophy
- Features should be modular and independent where it makes sense
- Primarily personal use, but potentially shared with others
- Prefer building on existing patterns in the codebase rather than introducing new ones

### External Documentation
- **`/BOT_HUB_INTEGRATION.md`** (repo root) — Integration guide for the Python IRC bot project. Describes how bots connect to the SignalR hub, register channels, log events, and receive commands. **Keep this file up to date whenever the hub API changes** (new hub methods, changes to `IrcEventRequest`, new `ChatAction` values, or changes to the `ReceiveCommand` contract).

### Reusable Components — check here before hand-rolling markup

**`Components/ResponsiveImage.razor`** — the app's one component for displaying an `ImageAttachment` (added 2026-09-07). Blazor doesn't have ASP.NET Core's Razor Pages/MVC "tag helper" concept, so this is the idiomatic equivalent: a component you drop into markup like any other MudBlazor tag. **Use this instead of writing a raw `<img>` for any `ImageAttachment`-backed image** — it generates the `srcset`/`sizes` pair from `EffectiveThumbnailWidth`/`EffectiveMediumWidth` correctly every time, which was previously hand-copied (with real width bugs, see the 2026-09-06 `srcset` sizing fix above) into 5+ separate files.

```razor
<ResponsiveImage Image="article.GetMainImage()" Alt="@article.Title" Height="160px" />
```

Parameters:
- `Image` (`ImageAttachment?`, required) — typically `entity.GetMainImage()`. Null renders `Placeholder` (or a generic image icon) instead of an `<img>`.
- `Alt` — alt text.
- `Height` (default `"200px"`) — CSS height of the box.
- `FixedHeight` (default `true`) — `true`: the box is always exactly `Height` tall, cropping/filling to match (uniform grid tiles — card grids). `false`: the box only caps at `Height` and shrinks to the image's own rendered height otherwise (a single non-tiled image — an article's hero image, where a short landscape photo shouldn't leave a tall empty box).
- `Fit` (`ResponsiveImage.ObjectFit.Cover` default, or `.Contain`) — Cover crops to fill (grid tiles, where showing the whole image isn't essential). Contain shrinks to fit entirely inside the box, letterboxed (anywhere cropping would hide real content — an article hero, a collectable's detail photo). **Getting this wrong is exactly the bug that prompted building this component**: the old hand-rolled `ArticleDetails.razor` hero image used Cover with a fixed height, which cropped a very different percentage of a portrait photo depending on viewport width (mobile showed most of the photo, desktop showed a tight, zoomed-in crop of it) — see the 2026-09-07 "article hero-image cropping" fix above for the full diagnosis.
- `Sizes` (default `"(max-width: 600px) 100vw, (max-width: 960px) 50vw, 33vw"`, matching this app's 3-cards-per-row desktop grid) — override for a different layout, e.g. a full-width hero: `"(max-width: 960px) 100vw, 960px"`.
- `Class` / `BorderRadius` — passed through to the wrapper.
- `Placeholder` (`RenderFragment?`) — rendered instead of the generic image icon when `Image` is null, e.g. `<Placeholder><MudIcon Icon="@Icons.Material.Filled.Warehouse" .../></Placeholder>` for a more specific empty-state icon per entity type.
- `OnClick` (`EventCallback`) — adds `cursor: pointer` and invokes the callback on click; used for click-to-zoom into `ImageModal`. Wrap in a `<div style="position: relative;">` alongside an absolutely-positioned overlay (e.g. a remove button) exactly like `ArticleDialog.razor`'s existing-images grid does — the component itself doesn't manage overlays, it's just the image box.

**Deliberately out of scope** (still hand-rolled `<img>`, and should stay that way): `ImageModal.razor`'s own full-size lightbox display (wants the single largest image, not a responsive srcset choice); `AppShell.razor`'s static logo (not an `ImageAttachment` at all); `AddCollectable.razor`/`EditCollectable.razor`'s "newly selected, not yet uploaded" file preview grids (JS-generated blob URLs, no `ImageAttachment` exists yet to hand the component); `CollectionDetails.razor`'s 60×60 avatar-style thumbnail in the "Add Collectable" picker list (too small to benefit from responsive sizing, and the component's flex-centered wrapper doesn't currently support an explicit pixel width the way that spot wants — would need a `Width` parameter added first if a similar tiny-avatar spot comes up again).

**If you add a new place that displays an `ImageAttachment`-backed image, use `ResponsiveImage` — don't hand-write another `<img src srcset sizes>` block.** If it can't cover a genuinely new case, extend the component's parameters (and update this doc) rather than reintroducing a duplicate, un-DRY block elsewhere.

---

## Working Checkpoint

*Last updated: 2026-09-07*

### Current State
- Collections feature exists and is functional
- IRC Chat monitoring page is implemented for Admins, with real-time updates via a SignalR-backed notifier
- IRC events are logged via REST API (`POST /api/irc/events`) from a Python bot
- Chat view has a mIRC-style retro UI with a channel sidebar, network selector, and unread indicators (red channel names for activity on non-selected channels)
- Dev environment now also builds/runs on Linux (Linux Mint 22.3), in addition to the original Windows setup
- **UI has been fully migrated from Bootstrap to MudBlazor** (6.21.0). Bootstrap CSS/Icons and the vendored `wwwroot/bootstrap/` files have been removed entirely — every page now renders through MudBlazor components. Custom dark theme is defined in `Components/Layout/AppMudTheme.cs`, mapped 1:1 from `app.css`'s existing `:root` CSS variables; providers live in `Components/Layout/MudProviders.razor`. The app shell (`MudAppBar`/`MudDrawer`/`MudNavMenu`) is `Components/Layout/AppShell.razor`, invoked as a single `@rendermode="InteractiveServer"` island from `MainLayout.razor` — this replaced the old hand-rolled sidebar/mobile-nav and its `IJSRuntime.InvokeVoidAsync("eval", ...)` margin-toggle hack entirely.
- **Important constraint discovered during the migration**: `MudTextField`/`MudCheckBox`/other Mud form inputs do NOT render an HTML `name` attribute, so they cannot bind through Blazor's static-SSR `EditForm method="post"` + `[SupplyParameterFromForm]` model. All of ASP.NET Core Identity's scaffolded `/Account` pages render as static SSR (required for cookie-setting POST forms) — so those pages keep the native `InputText`/`InputCheckbox`/etc. (with their original Bootstrap-era CSS classes) for every actually-bound form field, and only use MudBlazor components for layout/buttons/alerts/headings around them. Same reasoning applies to a handful of native `<button name=... value=...>`/`<button form="...">` elements elsewhere in the Account area (external-login provider selection, `Email.razor`'s send-verification button) — deliberately left un-migrated since they rely on raw form attributes MudButton doesn't replicate. If a future page needs form-bound inputs under static SSR, don't reach for Mud input components there.
- **Post-migration bug fixed (2026-08-30)**: after the MudBlazor migration, the app loaded but the hamburger menu/drawer was completely unresponsive. Root cause: `MudPopoverProvider` (inside `Components/Layout/MudProviders.razor`) threw `InvalidOperationException: Duplicate MudPopoverProvider detected` on every page load, which crashed the entire Blazor Server circuit immediately after connecting (visible in browser console as "unhandled exception on the current circuit" + silent disconnect) — killing ALL interactivity on the page, not just the menu, since the whole circuit died. Root topology cause: `MainLayout.razor` renders multiple independent `@rendermode="InteractiveServer"` islands side by side (`MudProviders`, `AppShell`, and `@Body`'s own per-page render mode) — a valid but non-default arrangement that trips MudBlazor's duplicate-provider guard as a false positive even though `MudPopoverProvider` is declared exactly once. Fixed in `Program.cs` by configuring `AddMudServices(config => config.PopoverOptions.ThrowOnDuplicateProvider = false)` — this is MudBlazor's own documented escape hatch for this exact check (named directly in its error message). Also fixed in the same pass: `AppMudTheme.cs`'s `PaletteDark` had no `Secondary` color set, so every `MudText`/`MudButton` etc. using `Color="Color.Secondary"` (used throughout for muted text) rendered in MudBlazor's stock pink/magenta default instead of the intended muted gray — fixed by setting `Secondary = "#8b949e"` to match `TextSecondary`. If new MudBlazor circuit-crash or stray-default-color symptoms show up after further UI changes, check these two spots first.
- **Post-migration bug fixed (2026-08-30)**: the drawer, when opened, was partially covered by the app bar at the top instead of sliding out fully below it. Root cause, confirmed via computed styles in the browser: MudBlazor's default z-index is `Drawer=1100` vs `AppBar=1300` (both `position:fixed`), AND separately the `mud-drawer-clipped-always` CSS rule that's supposed to offset the drawer's `top` to `var(--mud-appbar-height)` wasn't matching in this app's DOM structure — the drawer's computed `top` was `0px` even though `--mud-appbar-height: 64px` was correctly set at the layout root, so the drawer extended full-height from the very top of the viewport, behind the app bar. Fixed with an explicit override in `wwwroot/app.css`: `.mud-drawer.mud-drawer-clipped-always { top: var(--mud-appbar-height) !important; height: calc(100% - var(--mud-appbar-height)) !important; }`. This is a plain CSS file — changes here take effect on a browser refresh, no rebuild needed. `AppShell.razor` uses `ClipMode="DrawerClipMode.Always"` on the `MudDrawer`; if that's ever changed, re-verify this override still matches.
- **Post-migration bug fixed (2026-08-30)**: on short-content pages (e.g. a simplified `Home.razor`), the drawer/nav sidebar and dark background only extended as far as the page content instead of reaching the bottom of the viewport. Root cause: MudBlazor's `.mud-layout { height: 100% }` only resolves to a real pixel value if its ancestor chain (`body` → `html`) has a *definite* height — `app.css`'s `html, body` rule never set one, so the chain collapsed to content-height (`auto`) instead of the viewport. Fixed by adding `height: 100%;` to the existing `html, body` rule in `wwwroot/app.css`. Verified via `getBoundingClientRect()` in the browser: drawer/layout/body/html heights all now match `window.innerHeight` exactly regardless of content length, on both interactive (MudLayout) and static-SSR (Identity/AccountLayout) pages. Static CSS file — takes effect on refresh, no rebuild needed.
- **Sticky page-footer pattern established (2026-08-30)**: to let a page pin something (e.g. `HitCounter`) to the bottom of the viewport regardless of content length, `wwwroot/app.css` makes `.mud-main-content` a flex column with `min-height: calc(100vh - var(--mud-appbar-height))`, and `.mud-main-content > .content` (the `<article class="content">@Body</article>` wrapper in `MainLayout.razor`) a flex column filling it via `flex: 1 1 auto`. A page opts in by rendering a `<footer class="page-footer">...</footer>` as a **sibling** of its main content (not nested inside it) — `.page-footer`'s `margin-top: auto` then pushes it to the bottom of the available space. See `Components/Pages/Home.razor` for the reference usage (`<MudContainer>...</MudContainer>` followed by `<footer class="page-footer"><HitCounter PagePath="/" /></footer>` as two separate root-level elements). No Blazor plumbing (no `SectionOutlet`/cascading service) was needed since this is pure CSS — reuse the same `<footer class="page-footer">` sibling pattern on any other page that wants a sticky footer.
- **User preference noted (2026-08-30)**: lean on MudBlazor component props (e.g. `MudStack`'s `Justify`/`AlignItems`) instead of adding custom CSS when the component already exposes the needed behavior natively. Came up when centering `HitCounter`'s content — `.page-footer`'s `text-align: center` in `app.css` was inert (doesn't affect a flex row's main-axis alignment) and was replaced with `Justify="Justify.Center"` on `HitCounter.razor`'s `MudStack`, and the dead CSS rule was deleted. Saved as a standing preference in memory (`feedback_mudblazor_native_styling`) — check whether a MudBlazor prop already covers a styling need before reaching for `app.css`.
- The mIRC retro chat theme (`.mirc-*` classes in `app.css`) was preserved by applying the same class names onto MudBlazor component wrappers (`MudPaper`/`MudToolBar`/`MudText`) rather than rewriting the CSS — the riskiest elements for this trick (the network `<select>` and the message `<input>`) were deliberately left as native HTML elements rather than swapped for `MudSelect`/`MudTextField`, since those add wrapping DOM that could fight the `!important`-heavy retro CSS and couldn't be visually verified (no browser automation was available in that session).
- **Two mobile-nav bugs fixed (2026-08-30), found from real device screenshots**: (1) opening the drawer on mobile made the app bar (hamburger + logo) visually disappear — MudBlazor renders the drawer's temporary-mode backdrop/scrim at `z-index: appbar+1` *by design*, and further bumps the open drawer's own z-index dynamically to sit above that scrim (observed at appbar+2, i.e. overlay+1) so its links stay clickable — meaning a small app-bar z-index bump ties with the drawer instead of clearing it. Fixed in `wwwroot/app.css` by forcing `.mud-appbar { z-index: calc(var(--mud-zindex-appbar) + 10) !important; }`, a wide-enough margin to clear both regardless of MudBlazor's exact drawer/scrim values. (2) The mobile drawer didn't auto-close after picking a nav item — `MudNavLink` renders as a plain `<div>` (not a real `<a href>`), so its `OnClick`-based close signal (`AppShell.razor`'s `OnNavigate` callback, invoked from `NavMenu.razor`'s `NotifyNavigate`) was racing the actual navigation. Fixed by also invoking `OnNavigate` from `NavMenu.razor`'s existing `Navigation.LocationChanged` subscription, which only fires once navigation has genuinely completed — a reliable signal regardless of the click-timing race. Both fixes were verified against the real Blazor Server app (not guessed) using an iframe injected into a Chrome tab at real mobile dimensions (390×844) — `resize_window`/CDP viewport emulation don't actually shrink this environment's browser viewport, but a same-origin iframe genuinely gets its own `window.innerWidth`/media-query breakpoint, which is what MudBlazor's own JS breakpoint detection reads. Within that iframe, real `computer`-tool mouse clicks didn't reliably reach nested-iframe content (a CDP/tooling limitation, not an app bug) — DOM-level `element.click()` calls inside the iframe's own document worked fine and were used for the actual click-and-observe verification.
- **The stock `dotnet new blazor` example "Auth Required" page was removed (2026-08-30)**: deleted `Components/Pages/Auth.razor` and its `MudNavLink` in `NavMenu.razor`. It was fully self-contained (grepped, nothing else referenced it).
- **`/admin/allUsers` redesigned (2026-08-30)**: the old `MudTable` inline-editing UI was architecturally broken — the edit fields (`editUserEmail`/`editUserName`/`editPassword`/`editUserLocked`) were single shared scalar fields (not per-row state) with only one inconsistent `@key` in the whole row template, so editing state could bleed across rows. Replaced with a modal dialog, `Components/Pages/EditUserDialog.razor` — a new, page-specific component following `ImageModal.razor`'s convention (`@ref` + public `ShowModal(user)` method, inline-bound `<MudDialog @bind-IsVisible>`). This also adds a **Roles tab** (a `MudTabs` alongside "General") with a toggle switch per `IdentityRole` (only "Admin" exists today — no role-creation UI was added, by design) — the first UI in the app that lets an admin actually grant/revoke roles; previously roles were seed-only (`Services/DatabaseSeeder.cs`) and displayed read-only. `AllUsers.razor` itself is now purely a read-only list + "Edit"/"Activate" action buttons; all the old inline-edit fields/methods and the dead `SignInManager`/`IJSRuntime` injections were removed.
  - **Reusable pattern for a submit button living in a `MudDialog`'s `DialogActions` (outside the `DialogContent`)**: give the `<EditForm>` an `id` (its `AdditionalAttributes` — confirmed via reflection — passes it through to the rendered `<form>`), then use the HTML `form="<that id>"` attribute (also passes through MudButton's own unmatched-attribute capture) on the `<MudButton ButtonType="ButtonType.Submit" form="...">` placed in `DialogActions`. This is required because `DialogActions` must be a sibling of `DialogContent` (both direct children of `<MudDialog>`, not nested inside each other), so a submit button in `DialogActions` can't be a descendant of the `<EditForm>` in `DialogContent` — the `form=` attribute is the standard HTML mechanism for a button to submit a form it isn't nested inside. See `EditUserDialog.razor` for the reference implementation.
  - Not independently browser-verified end-to-end (opening the dialog, toggling roles, saving) — doing so requires signing in as the seeded Admin account, and entering that account's password into the login form is something Claude won't do regardless of whose local app it is. Verified instead via build success, HTTP-level routing checks (`/admin/allUsers` still correctly redirects unauthenticated, `/auth` now 404s), and code review. **Give this page a manual pass** the next time you're signed in as Admin.
- **`EditUserDialog.razor` updated (2026-08-30)**: added an "Email Confirmed" switch next to "Account Locked" on the General tab (wired to `ApplicationUser.EmailConfirmed`). Also fixed the dialog resizing to fit whichever tab was shorter (Roles, with only one role today, is much shorter than General) — `MudTabs` got a `PanelClass="edit-user-tab-panel"`, and `app.css` sets `min-height: 281px` on it (measured from the General tab's actual rendered height), so switching tabs no longer changes the dialog's size. **Technique for testing auth-gated components without logging in**: temporarily added an unauthenticated `@page` (no `[Authorize]`) that rendered `EditUserDialog` directly with a synthetic in-memory `ApplicationUser` (never persisted — `UserManager.GetRolesAsync`/`RoleManager.Roles` work fine against a non-existent user id, just return empty), verified in the browser, then deleted the temp page before committing. Avoids ever needing the seeded Admin account's password. Also: browser CSS caching can mask a static file's real content even when the server is confirmed serving the updated version — a `link.href += '?v=' + Date.now()` swap forces a true reload of just that stylesheet without a full page refresh.
- **Roles tab switched from toggles to a drag-and-drop `MudDropContainer` (2026-08-30)**: replaced the per-role `MudSwitch` list on `EditUserDialog.razor`'s Roles tab with MudBlazor's documented "Basic Usage" dropzone pattern — two `MudDropZone<RoleDropItem>` zones ("Available"/"Assigned") inside one `MudDropContainer<RoleDropItem>`, keyed by a `RoleDropItem.Identifier` string field (`ItemsSelector="(item, zone) => item.Identifier == zone"`, updated on drop via `ItemDropped`). `ShowModal` now builds `_roleItems` from all role names with `Identifier` set to "Assigned"/"Available" based on `_originalRoles`; `HandleSave`'s add/remove-role diff loop is unchanged in spirit, just reads `item.Identifier == "Assigned"` instead of a dictionary bool. Note the nested-generic-RenderFragment gotcha: `MudDropContainer`'s `ItemRenderer` needed an explicit `Context="roleItem"` since its implicit `context` collides with the enclosing `EditForm`'s `ChildContent` context — a `RZ9999` compile error otherwise. Verified end-to-end in the browser with the same synthetic-user temp-page technique above (drag "Admin" between zones, confirmed it moves); the existing `min-height: 281px` dialog-height fix held up fine with the new content. Re-hit the browser CSS caching gotcha from above (`link.href += '?v=' + Date.now()` fixed it again) — worth remembering this bites every time a CSS change is being verified live.
- **`/admin/allUsers` rebuilt again (2026-08-30), this time as a card grid**: replaced the `MudTable` with a responsive `MudGrid` of `MudCard`s (one per user) — `MudCardHeader` has a `MudAvatar` showing the user's first-initial (from `DisplayName`, falling back to `UserName`/`Email`) plus name/email; `MudCardContent` holds Created date + Locked/Confirmed/Role chips; `MudCardActions` has just an Edit and a Delete `MudIconButton` (wrapped in `MudTooltip` for the hover label — the codebase had no prior tooltip-on-icon-button convention). The old bulk-select checkboxes + "Activate Selected" bar were removed entirely per explicit user instruction — email confirmation is handled solely through `EditUserDialog`'s existing "Email Confirmed" toggle now, no per-card or bulk activate action exists on this page. A plain `MudTextField` (`Immediate="true"`, `Clearable="true"`, search-icon adornment) does live client-side filtering across `DisplayName`/`UserName`/`Email` (case-insensitive `Contains`) — first search-box pattern in the app, nothing to reuse existed.
  - **New reusable `Components/ConfirmDialog.razor`** — first "are you sure?" `MudDialog` in the codebase (everything prior used raw JS `confirm()`/`alert()` via `IJSRuntime`, or a fake no-op stub in `Collectables.razor`). Follows the same `@ref` + `ShowModal(...)` + `EventCallback` convention as `EditUserDialog`/`ImageModal`: `ShowModal(string title, string message, string confirmText, Color confirmColor)` sets state and opens; `OnConfirmed` (parameterless `EventCallback`) fires on the confirm button. Generic/page-agnostic by design — reuse it for other destructive confirmations instead of writing another one-off.
  - **Delete is a real hard delete** (`UserManager.DeleteAsync`) — first admin-facing user deletion in the app (previously `UserManager.DeleteAsync` only existed in the self-service `DeletePersonalData.razor` flow). `Collectable`/`CollectableCollection` both cascade-delete on their `UserId` FK (`ApplicationDbContext.cs`), so deleting a user also wipes their collections/collectables/images — the confirm dialog's message says this explicitly so the admin isn't surprised. `AllUsers.razor` stores the pending user in `_userPendingDelete` when the delete icon is clicked, then commits the deletion from `ConfirmDialog`'s `OnConfirmed` callback.
  - Verified end-to-end in the browser against the **real** seeded Admin account (not a synthetic user this time, since deleting/editing needed to be checked against real data) by temporarily commenting out `@attribute [Authorize(Roles = "Admin")]` to view the page unauthenticated, then restoring it before the final build/commit. Confirmed: card renders correctly, live search filtering works, Edit dialog still opens correctly, and the Delete confirm dialog shows the correct cascade-delete warning text — **deliberately never clicked the dialog's actual Delete button**, since the only user in the local dev DB is the real seeded Admin account; only Cancel was exercised. No admin-role or multi-user deletion path has been exercised with real data as a result — worth a manual smoke test with a disposable test user before relying on this in production.
- **`/admin/allUsers` card layout refined (2026-08-30)**: moved the Edit/Delete `MudIconButton`s into `MudCardHeader`'s `CardHeaderActions` slot (top-right of the card, next to the avatar/name, instead of a separate footer row — `MudCardActions` was removed from the card entirely since it had no other content). Reordered `MudCardContent` so the Locked/Confirmed status chips render above the Created-date line (previously date was first). Header content now shows three separate lines — `UserName` (with a small `Icons.Material.Filled.AdminPanelSettings` badge icon next to it, shown only when `userRoles` contains "Admin", via a new `IsAdmin(user)` helper), then `DisplayName` (only rendered when set), then `Email` — instead of the old single "display name or fallback" title line. The search box (`FilteredUsers` in the code-behind) already matched against `DisplayName` before this change, so no filtering logic needed updating, only the missing on-card visibility. Not independently browser-verified for the `DisplayName` line specifically — the only local user (seeded Admin) has no `DisplayName` set and `EditUserDialog` has no field to set one — but it's the same simple `string.IsNullOrWhiteSpace` conditional pattern already proven elsewhere on this page (Admin badge, roles-chip fallback), so it's confidently correct. Admin badge and header-action relocation were both directly verified in the browser against the real seeded Admin account (same temporary-`[Authorize]`-removal technique as above).
- **Admin indicator switched to a `MudBadge` + card-header overflow bug fixed (2026-08-30)**: per MudBlazor's [badge playground docs](https://mudblazor.com/components/badge#playground), the plain `MudIcon` next to the username was replaced with `<MudBadge Icon="@Icons.Material.Filled.AdminPanelSettings" Color="Color.Warning" Origin="Origin.TopLeft" Overlap="true">` wrapping the username `MudText`, itself wrapped in a `MudTooltip Text="Admin"`. The wrapped `MudText` needed `Class="pt-3 ps-4"` **padding** (not margin) on the `MudBadge` itself — margin would've shifted the whole anchor box without creating room *inside* it, so the badge circle would still sit directly over the first letters; padding pushes the visible text inward, leaving clear space for the corner badge. Also removed the Roles chip list from `MudCardContent` entirely (was redundant now that Admin has its own badge, and no other roles exist in the app).
  - **Real bug found and fixed**: the Edit/Delete icons in `CardHeaderActions` were rendering past the card's right edge on cards with long email addresses. Root cause (confirmed via `getBoundingClientRect()`/`getComputedStyle()` in the browser, not guessed): MudBlazor's `.mud-card-header-content{flex:1 1 auto}` is a flex item whose default `min-width` is `auto`, which — per the standard CSS flexbox behavior — stops it from ever shrinking below its own content's intrinsic width. An unbroken email string has no wrap points, so the content refused to shrink and pushed `.mud-card-header-actions` outside the card. Setting `min-width:0` on a *descendant* div did nothing, since the actual flex item is `.mud-card-header-content` itself, generated by MudBlazor — not something `CardHeaderContent`'s child markup can reach directly. Fixed by adding `Class="user-card-header"` to `MudCardHeader` and a scoped rule in `wwwroot/app.css`: `.user-card-header .mud-card-header-content { min-width: 0; }` (confirmed via grep this is the only page using `MudCardHeader`, so the rule can't affect anything else). Combined with the existing `white-space: nowrap; overflow: hidden; text-overflow: ellipsis;` inline styles on the header's `MudText`s, the email now truncates with an ellipsis instead of overflowing. **Re-hit the recurring browser-CSS-caching gotcha** verifying this — `curl`ing `/app.css` showed the server had the fix immediately, but the open tab kept rendering with the stale rule until forcing a true reload via `link.href += '?v=' + Date.now()`; this has now bitten on three separate CSS-only verification passes in this app and is worth remembering reflexively whenever a live CSS change "isn't working."
- **User-card padding tightened (2026-08-30)**: MudBlazor's default 16px padding on both `.mud-card-header` (bottom) and `.mud-card-content` (top) was stacking into a visibly empty gap between the header and the Locked/Confirmed chip row. Added a `user-card-content` class to `MudCardContent` (alongside the existing `user-card-header` on `MudCardHeader`) and two new scoped rules in `wwwroot/app.css`: `.user-card-header { padding-bottom: 6px; }` / `.user-card-content { padding-top: 6px; }` — each more than halving the 16px default, per explicit request. Verified via `getComputedStyle()` in a fresh (uncached) tab that both resolved to `6px`.
- **Admin badge moved to the avatar; card grid widened for longer emails (2026-08-30)**: the `MudBadge` (see the badge-adoption entry above) now wraps `CardHeaderAvatar`'s `MudAvatar` instead of the username `MudText` — `<MudTooltip Text="Admin"><MudBadge ...><MudAvatar>...</MudAvatar></MudBadge></MudTooltip>`. This was also the fix for a reported tooltip-position bug: `MudTooltip` anchors to the full bounding box of whatever it wraps, so wrapping the wide username line made the tooltip appear centered over that whole line (visually "in the middle of the card") — wrapping the small, fixed-size avatar instead anchors the tooltip tightly next to the cursor. The now-unneeded `pt-3 ps-4` padding hack (previously needed so the badge wouldn't overlap the username letters) was removed along with it, since overlapping an avatar's corner is the standard/expected badge look.
  - Also widened the card grid column from `lg="3"` to `lg="4"` (3 cards per row instead of 4 on large screens) because the email address — the longest text in the header — was truncating too aggressively. Measured precisely in the browser via `canvas.measureText()` against the email `MudText`'s actual font/available width rather than guessing: at `lg="3"` only ~20 characters fit before the ellipsis; at `lg="4"` ~34 fit — a gain of 14 characters, comfortably past the requested "about 10 more." `xs`/`sm`/`md` breakpoints are unchanged.

### Recent Decisions
- Removed channel dropdown from the titlebar; channel selection is now only via the sidebar list
- Network selector moved into the channel list panel, centered above the channel list
- Unread channel tracking is keyed by `(network, channel)` tuple so switching networks preserves unread state for all networks
- Repo layout is nested: git root is `PatinaBlazor/`, which contains `PatinaBlazor.sln` and a `PatinaBlazor/` subfolder holding `PatinaBlazor.csproj`. `.vscode/` lives next to the `.sln` (one level below git root) — `workspaceFolder` in VS Code should be that `.sln`-containing folder, not the git root
- Fixed `.vscode/tasks.json`'s `build` task, which had a duplicated `PatinaBlazor/` path segment in the csproj target (`${workspaceFolder}/PatinaBlazor/PatinaBlazor/PatinaBlazor.csproj` → `${workspaceFolder}/PatinaBlazor/PatinaBlazor.csproj`) — this caused F5 debugging to fail with `MSB1009: Project file does not exist` even though Ctrl+Shift+B / manual builds worked. Same class of bug as the earlier `launch.json` fix noted below; both files reference paths relative to `workspaceFolder` and are easy to get out of sync when the nested repo layout changes.
- On Linux, local SQL Server is provided via Docker (`mcr.microsoft.com/mssql/server:2022-latest`, Developer edition) rather than SQL Server Express — keeps the same engine/EF Core SqlServer provider and existing migrations unchanged, so no separate migration history was needed
- The Linux-local SQL Server connection string is set via `dotnet user-secrets` (not committed to `appsettings.Development.json`), so the Windows machine's `Trusted_Connection=True` / named-instance config in that file is left untouched for when that machine is used

### Linux Dev Environment Setup (for reference / new machines)
- Docker installed via Docker's official apt repo (Ubuntu `noble`); user added to `docker` group
- SQL Server container: `docker run -e ACCEPT_EULA=Y -e MSSQL_SA_PASSWORD=... -e MSSQL_PID=Developer -p 1433:1433 --name patina-sql --restart unless-stopped -v patina-sql-data:/var/opt/mssql -d mcr.microsoft.com/mssql/server:2022-latest`
- Connection string set locally via `dotnet user-secrets set "ConnectionStrings:SqlServerConnection" "Server=localhost,1433;Database=DB_175541_silzellnet;User Id=sa;Password=...;TrustServerCertificate=True;"` (run from the `PatinaBlazor/PatinaBlazor/` project folder)
- `.vscode/launch.json` previously had a duplicated path segment in `program`/`cwd` (pointing at a nonexistent build output path) that broke F5 debugging — fixed to `${workspaceFolder}/PatinaBlazor/bin/Debug/net8.0/PatinaBlazor.dll`
- **Mailpit container for local email debugging (2026-09-07)**: `docker run -d --name patina-mailpit --restart unless-stopped -p 1025:1025 -p 8025:8025 axllent/mailpit` — a fake SMTP server that catches every email the app sends instead of delivering it, viewable at `http://localhost:8025`. `EmailSettings` pointed at it locally via `dotnet user-secrets set "EmailSettings:SmtpHost" "localhost"` / `SmtpPort` `1025` / `EnableSsl` `false` (Mailpit needs no real credentials; `SmtpUser`/`SmtpPassword` can be any non-empty placeholder) — same user-secrets-for-machine-specific-config convention as the SQL connection string above. The checked-in `appsettings.json` base `EmailSettings` (`localhost:587`, placeholder creds) was never a real listener; it's these user-secrets that make local sends actually go somewhere visible. Mailpit also has a built-in cross-client HTML-compatibility checker (the "HTML Check" tab on a message) - useful for any future email template work, not just this app's.

- **RV & Boat Storage feature added (2026-09-05), Phase 1 (admin-only)**: new data model in `Data/` — `StorageProperty` (name, address, description, `CreatedByUserId`/`ModifiedByUserId` + `CreatedDate`/`ModifiedDate`, the app's first use of a real creator/modifier audit-field pair, distinct from the existing bare-`UserId`-ownership pattern used by Collectables), `StoragePropertyImage` (mirrors `CollectableImage`), `StorageUnit` (unit number, length/width/height, `MonthlyRate` decimal(18,2), `StorageUnitStatus` enum: Available/Occupied/Reserved/Maintenance, string-converted), and `StorageRental` (links a unit to a customer `ApplicationUser`, `MonthlyRateAtSigning` snapshot, `StorageRentalStatus`: Active/Ended). `StorageRental` exists now (not deferred to Phase 2) purely so occupancy/revenue stats have real data to compute over, and so Phase 2 billing has a stable relationship to attach to later.
  - **Cascade-path gotcha hit and fixed**: SQL Server's error 1785 ("may cause cycles or multiple cascade paths") fires when a single table has *two or more* `SET NULL`/`CASCADE` foreign keys pointing at the *same* parent table — this hit `StorageProperty`/`StorageUnit`/`StorageRental`, each of which has both `CreatedByUserId` and `ModifiedByUserId` (plus `CustomerUserId` on `StorageRental`) all FKing to `AspNetUsers`. Fixed by making **all** of these audit/customer FKs `DeleteBehavior.Restrict` instead of mixing `SetNull`/`Restrict` — confirmed via `dotnet ef database update` actually failing on the first attempt with `SetNull`, then succeeding after the fix. Worth remembering if a future entity ever needs more than one FK to the same table with any cascading behavior.
  - New "Storage Admin" and "Storage Customer" roles added to `Services/DatabaseSeeder.cs` (extracted a small `EnsureRoleExistsAsync` helper since role creation was about to triple). Storage Admin is a scoped role (manages storage only); Admin remains a superset (`Authorize(Roles = "Admin,Storage Admin")` on every storage page). Confirmed **zero code changes needed** in `EditUserDialog.razor`'s Roles tab — it already builds its role list from `RoleManager.Roles`, so any new role appears there automatically.
  - Seeder also creates 2-3 dummy "Storage Customer" `ApplicationUser` accounts and calls a new `IStorageService.SeedDummyDataAsync` (idempotent, checks `StorageProperties.AnyAsync()`) to seed 2 properties, ~6-9 units each with varied sizes/rates/statuses, and a spread of rentals across the past year (some active, some ended) so the dashboard charts show real variation. Deliberately **no seeded images** — seeding real files would mean bypassing `IImageService`'s normal upload path, so the UI is just designed to handle an empty-image state instead (a warehouse placeholder icon).
  - New `Services/IStorageService.cs` / `StorageService.cs` (mirrors `CollectionService`'s shape) owns all CRUD plus dashboard aggregation (`GetDashboardSummaryAsync`, `GetMonthlyRevenueAsync`). Occupied/Available are **exclusively** service-managed via `StartRentalAsync`/`EndRentalAsync` (starting a rental also auto-assigns the customer the "Storage Customer" role); `UpdateUnitAsync` silently ignores an attempt to directly set `Status = Occupied`. Revenue projection is a deliberate flat-line of current MRR across future months, clearly labeled in the UI as a placeholder, not a real forecast.
  - New pages under **`Components/Pages/Storage/`** — the app's first feature-subfolder (everything else is flat under `Components/Pages/`), a deliberate small deviation matching the "modular features" design philosophy: `StorageDashboard.razor` (`/storage/admin` — stat cards + `MudChart` Donut for occupancy + `MudChart` Bar for monthly revenue, both colored from the existing `AppMudTheme.cs` palette, no new hex values), `StorageProperties.razor` (`/storage/admin/properties` — card grid + live search, same convention as `AllUsers.razor`), `StoragePropertyDetails.razor` (`/storage/admin/properties/{id}` — units table with Start/End Rental actions), and three dialogs (`StoragePropertyDialog`, `StorageUnitDialog`, `StorageRentalDialog`) all following the established `@ref` + `ShowModal(...)` + `EditForm id="..."` / `form="..."` submit-button-in-`DialogActions` convention from `EditUserDialog.razor`. `MudChart` (Donut/Bar/Line, `ChartSeries`/`ChartOptions`/`InputData`/`InputLabels`) is bundled in MudBlazor 6.21.0 itself — confirmed via reflection on the installed package — no new NuGet dependency was needed; this is the first chart in the app.
  - **Real bug found and fixed during browser verification**: all three dialogs' `HandleSave` originally did `user?.Id ?? string.Empty` for the current-user lookup, so if `AuthenticationStateProvider` ever returned no user, the code would attempt to save with `CreatedByUserId = ""` — an empty string isn't null, so it doesn't satisfy the nullable FK, and SQL Server rejects it with a raw FK-violation exception surfaced to the user. Fixed by checking `string.IsNullOrEmpty(currentUserId)` before attempting the save and showing a friendly "Please refresh and log in again" message instead, matching the defensive pattern already used in `AddCollectable.razor`. Found by testing the dialogs anonymously (see verification note below) — with `[Authorize]` in place this path is normally unreachable, but the fix is real and worth keeping.
  - `Home.razor` now redirects Storage-Admin-only users (in "Storage Admin" but not "Admin") straight to `/storage/admin` on load; full Admins keep the normal Home page and reach Storage via a new nav link (`NavMenu.razor`, gated `AuthorizeView Roles="Admin,Storage Admin"`) or a new "Storage Dashboard" quick-action button on `Admin.razor`.
  - **Verified in the browser** using the same temporary-`[Authorize]`-removal technique established earlier in this project for `EditUserDialog`/`AllUsers` (never entered the seeded Admin's password — entering login passwords is off-limits regardless of whose local app it is): dashboard charts, property grid, unit table, and all three dialogs render and behave correctly against the real seeded dummy data; confirmed the current-user-guard fix above by reproducing the original raw-exception failure, then the friendly message after the fix. `[Authorize]` attributes were restored immediately after and reconfirmed via `curl` that `/storage/admin`, `/storage/admin/properties`, and (unaffected) `/admin/allUsers` all correctly 302 to `/Account/Login` for anonymous requests. Not yet exercised by a real logged-in Admin session — worth a manual pass.
  - **Sandbox note for future sessions**: running the app via `dotnet run` in this dev container fails immediately (`SocketException: Cannot assign requested address`) because the default launch profile in `Properties/launchSettings.json` binds to a LAN IP (`192.168.1.116`) not present in this sandbox. Also, launching Kestrel via a plain backgrounded Bash call intermittently fails to bind even to `127.0.0.1` for no discernible reason (confirmed the OS/loopback itself is fine via raw socket tests) — retrying the exact same command, or using `dangerouslyDisableSandbox: true` with absolute paths for both the DLL and the log redirect, reliably works. Run the built DLL directly (`dotnet bin/Debug/net8.0/PatinaBlazor.dll`) with `ASPNETCORE_ENVIRONMENT=Development ASPNETCORE_URLS="http://127.0.0.1:<port>"` rather than `dotnet run`, to sidestep the launch-profile URL entirely.

- **Image handling unified across the whole app (2026-09-05)**: `Data/CollectableImage.cs` and `Data/StoragePropertyImage.cs` (two near-identical one-off entities) are gone, replaced by a single `Data/ImageAttachment.cs` used by every entity that has pictures. A same-day earlier pass had added responsive sizing as an opt-in `generateResponsiveSizes` flag scoped to Storage only — that flag is gone now; **every** image upload (Collectables included) always produces three JPEG variants sharing one GUID base name via `Services/ImageService.cs`'s `SaveImageAsync(file, subfolder)`: `{guid}.jpg` (large, max 2048px), `{guid}_medium.jpg` (max 1000px), `{guid}_thumb.jpg` (max 400px) — normalized to JPEG regardless of the uploaded format.
  - **New `Data/ISupportImageAttachments.cs`**: interface implemented by `Collectable` and `StorageProperty` (and any future entity with pictures), exposing `ImageSubfolder` (the on-disk/URL subfolder name — was previously a magic string like `"collectables"`/`"storageproperties"` repeated at every call site; now each entity has a `public const string ImageSubfolderName` backing an interface-facing `ImageSubfolder` instance property) and `ICollection<ImageAttachment> Images`. A `GetMainImage()` extension method (`.OrderByDescending(IsMainImage).ThenBy(DisplayOrder).FirstOrDefault()`) replaces the same inline LINQ that used to be duplicated at every display call site.
  - **`ImageAttachment` fields**: `FileName`, `RelativePath` (web-URL-ready, e.g. `/uploads/collectables/{guid}.jpg`, also used for on-disk deletion after trimming the leading slash), `ThumbnailRelativePath`/`MediumRelativePath` (nullable — null only for the rare non-decodable-image fallback path), `ContentType`, `FileSize`, `IsMainImage`, `DisplayOrder`, `CreatedDate`, plus **one nullable FK per owner type** (`CollectableId` / `StoragePropertyId` today) each with its own `Cascade` delete — adding a third owner type later means adding one more nullable FK column + one more `HasOne(...).WithMany(...)` pair in `ApplicationDbContext`, never a new image table. `[NotMapped]` computed `ThumbnailUrl`/`MediumUrl` properties fall back to `RelativePath` when a variant wasn't generated, so display code never needs a null-check. A DB-level `CHECK` constraint (`CK_ImageAttachment_ExactlyOneOwner`) enforces exactly one owner FK is set — verified this actually rejects a dual-owner insert, not just documented as an intent.
  - `Services/ImageService.cs` lost its `CollectableImage`-typed overloads (`GetImageUrl(CollectableImage)`, `DeleteCollectableImageAsync`) entirely — `GetImageUrl`/`GetThumbnailUrl`/`GetMediumUrl` are gone from the service altogether, since `ImageAttachment` now stores its own ready-to-use paths; pages just read `image.RelativePath`/`.ThumbnailUrl`/`.MediumUrl` directly. `DeleteImageAsync` has two overloads: `(ImageAttachment)` for a persisted row, and `(ImageUploadResult)` for cleaning up a just-saved-but-not-yet-committed file during a partial-batch-upload rollback (e.g. in `AddCollectable.razor`/`EditCollectable.razor`).
  - `IStorageService.AddPropertyImageAsync` now takes the whole `ImageUploadResult` instead of four loose positional strings — cleaner now that there are two extra path fields (thumbnail/medium) to plumb through.
  - Deleted `Components/CollectableCarouselCard.razor` — confirmed via repo-wide grep it was dead code (no `@page`, not referenced by any other component), so it was removed rather than half-updated to compile against the new type.
  - `Components/ImageModal.razor` is now genuinely entity-agnostic: its typed overload takes `List<ImageAttachment>` instead of `List<CollectableImage>`, so it's already reusable for a future Storage-property lightbox with no further changes (not wired up there yet — wasn't asked for).
  - **Migration `UnifyImageAttachments`** only *adds* the `ImageAttachments` table — see the next entry for why it deliberately does not drop `CollectableImages`/`StoragePropertyImages` (an initial version did, which was wrong: real environments will have rows in those tables by the time this ships).
  - **Verification**: (1) a standalone console harness (`ProjectReference`-ing the real `PatinaBlazor.csproj`) exercised `ImageService.SaveImageAsync` end-to-end for both a fake `Collectable` and a fake `StorageProperty` sharing the same `ImageAttachment` type, confirming all three sized files are produced and deleted correctly for both owners. (2) A second harness instantiated the real `ApplicationDbContext` against the actual dev SQL Server and ran the exact `.Include`/`.ThenInclude` chains `MyCollectables.razor`, `CollectionService`, and `StorageService` use, confirming they all still translate and execute correctly against the new schema; then did a real insert/constraint-violation/cascade-delete round trip against a throwaway row (cleaned up by the cascade delete itself) — confirmed the check constraint actively rejects a dual-owner insert and cascade delete actually removes the child row, not just that the migration ran. Both throwaway console projects were deleted after use. Did not verify through the actual browser UI (would require the seeded Admin's password); the isolated-harness technique from earlier in this session was reused here as the practical substitute.
- **Legacy image data migration added (2026-09-05, same day)**: the first cut of `UnifyImageAttachments` dropped `CollectableImages`/`StoragePropertyImages` outright — fine against the empty local dev DB, but wrong in general: any environment with real rows in those tables would lose them (and their DB-tracked association to a Collectable/StorageProperty) the moment this migration runs, since a raw EF migration can only run SQL and the whole point of unifying was to re-run every legacy image through `ImageService`'s SkiaSharp pipeline (which needs C#) to backfill the thumbnail/medium variants those old rows never had.
  - **Fix**: `UnifyImageAttachments`'s `Up()` now only creates `ImageAttachments`; it does not touch the two legacy tables (and `Down()` was trimmed to match — it only drops `ImageAttachments`, since `Up()` no longer creates the legacy ones for it to symmetrically remove). New `Services/ImageAttachmentMigrationService.cs`, invoked once from `Program.cs` right after `context.Database.MigrateAsync()` and before `DatabaseSeeder.SeedAsync()`, does the actual conversion at app startup: for each legacy table it still finds (checked via `INFORMATION_SCHEMA.TABLES`, so this is automatically a no-op forever once both are gone), it reads every row via raw ADO.NET (the old `CollectableImage`/`StoragePropertyImage` C# classes no longer exist, so this can't go through EF's normal `DbSet` machinery), runs each row's on-disk file through a new `IImageService.SaveImageFromDiskAsync(sourceFilePath, subfolder)` to produce fresh large/medium/thumb variants, and inserts a new `ImageAttachment` preserving the original `CreatedDate`/`IsMainImage`/`DisplayOrder`/owner FK. Only if every row in a table converts successfully does it drop that table via raw SQL (`DROP TABLE`) in the same pass — so a real production push converts-then-cleans-up in one deploy, but a partial failure leaves the untouched legacy table in place to retry on the next startup rather than losing data.
  - `Services/ImageService.cs` gained `SaveImageFromDiskAsync` alongside the existing `IBrowserFile`-based `SaveImageAsync` — both now funnel through a shared private `SaveFromStreamAsync(Func<Stream> openStream, ...)` core (a stream *factory*, not a single stream, so the non-decodable-image fallback path can safely re-open a fresh read rather than needing the first stream to support seeking — `IBrowserFile`'s stream in Blazor Server isn't guaranteed to).
  - **Real bug caught during verification, unrelated to the migration logic itself**: the very first test run threw `ArgumentNullException` on `_environment.WebRootPath` inside the new service — not a code bug, but this sandbox's shell `cwd` having drifted to the git root (`PatinaBlazor/`) instead of the project folder (`PatinaBlazor/PatinaBlazor/`) before launching `dotnet PatinaBlazor.dll`, which made ASP.NET Core's default content-root resolution (`Directory.GetCurrentDirectory()`) pick the wrong folder and fail to find `wwwroot`. Fixed for testing purposes by passing `--contentroot <project-folder>` explicitly rather than relying on shell cwd — worth remembering if a future from-scratch run of the built DLL (as opposed to `dotnet run`, which handles this itself) throws the same way.
  - **Verified against real simulated production conditions**, not just empty tables: inserted a real `Collectable` + a `CollectableImages` row (dated 2025-01-15) and a `StoragePropertyImages` row against an existing seeded property (dated 2025-02-20), both pointing at real physical PNG files placed at their expected legacy paths — then ran the actual app startup and confirmed via direct SQL: both legacy tables were dropped, both rows became `ImageAttachment`s with correctly-generated large/medium/thumb files on disk, and — importantly — the original `CreatedDate`s were preserved exactly (not overwritten with the migration's own run time). Also confirmed a second startup is a silent no-op (both tables already gone). All test rows/files were removed afterward; nothing from this verification pass remains in the dev DB or `wwwroot`.

- **Storage feature + image unification pushed to production (2026-09-05)** and confirmed working — `ImageAttachmentMigrationService` successfully converted real legacy image rows on the real production push, exactly as verified locally beforehand.
- **EXIF orientation bug found and fixed (2026-09-05, after the production push)**: some existing pictures came out rotated -90° after being converted by `ImageAttachmentMigrationService`. Root cause: `SKBitmap.Decode` ignores a photo's EXIF `Orientation` tag entirely (phones/cameras commonly store raw sensor-orientation pixels and rely on that tag for correct display), and re-encoding via SkiaSharp doesn't carry the tag forward either - so any image with the tag set came out baked-in-wrong, silently. This affected every code path, not just the migration (the same `SaveFromStreamAsync` is used for the migration, `AddCollectable`/`EditCollectable`, and both Storage dialogs).
  - **Fix**: `Services/ImageService.cs`'s `SaveFromStreamAsync` now decodes via `SKCodec` (not just `SKBitmap.Decode(SKData)`) to read `codec.EncodedOrigin`, then a new `ApplyExifOrientation` helper bakes the correct rotation/mirror into the pixel data itself *before* resizing/encoding, via an `SKCanvas` transform matrix (translate+rotate or translate+scale depending on orientation) - covers all 8 possible EXIF orientation values, with a same-instance short-circuit (no-op, no copy) for the common already-correctly-oriented case.
  - **Verification**: generated real JPEGs with actual EXIF `Orientation` tags (1,2,3,4,6,8 - the 6 values real cameras/scanners actually produce; 5 and 7 don't occur in practice) via Python/Pillow, each with an asymmetric red/blue corner-marker pattern so any rotation or mirror is unambiguous. Used Pillow's own trusted `ImageOps.exif_transpose()` as ground truth for where each marker should end up and what the corrected dimensions should be. Compared pixel-for-pixel against (1) the isolated `ApplyExifOrientation` matrix logic and (2) the real end-to-end `ImageService.SaveImageAsync` (via the by-now-familiar `ProjectReference`-a-throwaway-console-app technique) - both matched Pillow's ground truth exactly for all 6 orientations, including the 90°/-90° cases that triggered the original report. All test images/harnesses deleted after verification.
  - **Not fixed**: the already-migrated production images that came out rotated - user is handling those manually. This only prevents it from happening to any future upload or re-migration.
  - Committed (2026-09-05) but not yet pushed at the user's request, pending the billing-frequency change below going in first.
- **Billing frequency added to rentals (2026-09-05)**: `StorageRental` gained a required `BillingFrequency` enum (`Monthly`/`Quarterly`/`Annually`, string-converted, DB default `'Monthly'` so the migration backfills existing rows safely) — independent of `MonthlyRateAtSigning`, which stays a monthly-equivalent figure for revenue/MRR reporting regardless of how often the customer is actually invoiced. `IStorageService.StartRentalAsync` takes the new parameter; `StorageRentalDialog.razor`'s Start mode has a "Billing Frequency" `MudSelect` (default Monthly, renamed from an initial "Bill Customer" label per user feedback) plus a live caption showing the actual per-period charge (e.g. "$1,320.00 every 12 months" for a $110/mo unit billed annually). `StoragePropertyDetails.razor`'s unit table has its own "Billing Frequency" column right after Monthly Rate (moved there from a caption under the renter's name in Current Renter, per user feedback). Dummy rental seeding now assigns a realistic distribution (70% monthly, 20% quarterly, 10% annually).
  - **Verified**: persistence round-trip (write → read back through a fresh `DbContext`, no change-tracker cache → raw SQL column) confirmed correct for all three values via a throwaway console harness against the real dev DB (cleaned up after). Also visually verified in the browser (temporary `[Authorize]` removal, restored after) that the selector renders all three options and the caption updates reactively when either the frequency or the rate changes — confirmed against a real existing unit/property.
- **Start Rental's customer picker restricted to the Storage Customer role (2026-09-05)**: previously searched every site user via `UserManager.Users` (would surface Admins, Collectables-only accounts, anyone). `StorageRentalDialog.razor`'s `SearchCustomersAsync` now calls `UserManager.GetUsersInRoleAsync("Storage Customer")` and filters that list in-memory by the search text, instead of querying `.Users` directly. **Real workflow implication worth remembering**: since a user must already hold the Storage Customer role to appear in the picker at all, an admin now has to grant that role via `EditUserDialog`'s Roles tab *before* that person can be selected for a rental — the existing auto-assign-role-on-start logic in `StorageService.StartRentalAsync` is effectively a no-op safety net now, not the primary way someone gets the role. Verified live in the browser: the picker now lists exactly the 3 seeded dummy Storage Customers and nothing else (previously would have also shown the seeded Admin account).
  - **Gotcha hit and fixed while implementing this**: `@inject IStorageService StorageService` in this same file shadows the class name `StorageService`, so referencing `StorageService.StorageCustomerRoleName` (the role-name constant) resolved against the *injected instance* instead of the static class and failed to compile. Fixed with a fully-qualified `PatinaBlazor.Services.StorageService.StorageCustomerRoleName`. Worth remembering if this constant is ever referenced from another file that also injects `IStorageService` under the same name.
- All of the above (EXIF fix, billing frequency, label/column tweaks, customer-picker role restriction) were pushed to production on 2026-09-05.
- **Two real image-upload bugs found and fixed (2026-09-05), reported by the user as a generic Blazor circuit error** (`Uncaught Error: There was an exception invoking 'NotifyChange'...`) when uploading new photos to an existing collectable in production:
  1. **Pre-existing bug, unrelated to any recent work**: `AddCollectable.razor`/`EditCollectable.razor`'s client-side image preview passed the C# `IBrowserFile` object itself through `JSRuntime.InvokeAsync("createObjectURL", file)` - in Blazor **Server**, this only marshals the file's metadata as JSON, never a real browser `File`/`Blob`, so `URL.createObjectURL()` always threw `TypeError: Overload resolution failed`. Fixed by giving both pages' `<InputFile>` a `@ref`, reading its `.Element` (an `ElementReference?` - confirmed via reflection that `InputFile` exposes this), and passing *that* plus an index to a new JS helper (`createObjectURLFromInput`) that reads the real file straight off the native `<input>`'s own `.files[index]`. Safe to index this way only because `CreateImagePreviewUrls()` is always called immediately after `selectedImageFiles` was just freshly rebuilt from the same selection event - never after a later remove/reorder, which would desync the indices.
  2. **A real regression from this session's own SkiaSharp/responsive-image work**: `ImageService.SaveFromStreamAsync` called `SkiaSharp.SKData.Create(Stream)`, which does *synchronous* reads internally - Blazor Server's `BrowserFileStream` (what `IBrowserFile.OpenReadStream()` returns) only supports async reads and throws `NotSupportedException: Synchronous reads are not supported` on any sync `Read()` call. This meant **every real browser-driven image upload was broken** from the moment the responsive-sizing pipeline was introduced, across Collectables and Storage alike - it only went unnoticed because every verification pass in this session used a fake `IBrowserFile` backed by a real `File.OpenRead()` `FileStream` (which supports sync reads fine), never Blazor's actual constrained stream. Fixed by buffering the stream into a `MemoryStream` via `CopyToAsync` (async-safe) first, then handing SkiaSharp the in-memory copy (sync-read-safe, since it's not doing real I/O). The legacy-image migration path (`SaveImageFromDiskAsync`, backed by a plain `File.OpenRead`) was never affected by this specific bug, which is why it verified fine earlier.
  - **Verification**: reproduced both errors locally end-to-end - via a real `EditCollectable` page temporarily made reachable without login (auth attribute disabled, `OnInitializedAsync` pointed at the seeded Admin's id directly - both reverted after) against a real throwaway `Collectable` row, with `CircuitOptions.DetailedErrors` temporarily enabled (reverted after) to get past the deliberately generic client-facing message and see the actual stack trace, which is what pinpointed both root causes precisely rather than guessing. After both fixes, repeated the exact same repro (real file picked into the real `<input>`, real save submit) and confirmed via direct DB/disk inspection that the `ImageAttachment` row and all three resized files were created correctly. All temporary code, test DB rows, and test files were removed afterward.
  - **Lesson for future service-layer verification**: a fake `IBrowserFile` wrapping a real `FileStream` is *not* an adequate stand-in for Blazor Server's real upload stream when the code path does any synchronous I/O - it will hide exactly this class of bug. Prefer testing upload-triggered code through an actual browser session (even via the temporary-auth-bypass technique) at least once per change, rather than trusting an isolated harness alone, whenever the change touches how an `IBrowserFile`'s stream is read.
  - Both image-upload fixes above were committed and pushed to production on 2026-09-05, confirmed working by the user ("This works!").
- **`PaymentDate` (recurring billing anchor) added to rentals (2026-09-05)**: groundwork for the eventual PayPal/invoicing integration the user wants to design incrementally — `Invoice`/`Payment`/`Receipt` entities are explicitly deferred until a payment provider is picked, since their shape should be informed by that provider's API. `StorageRental` gained a required `PaymentDate` (the day recurring charges land on) and a `GetNextBillingDate(DateTime asOf)` instance method that returns the next due date on/after `asOf`, preserving `PaymentDate`'s original day-of-month across cycles. Per explicit user constraint, `PaymentDate` must be within `[StartDate, StartDate + 1 month]` — validated both client-side in `StorageRentalDialog.razor` (`_errorMessage`, before calling the service) and server-side in `StorageService.StartRentalAsync` (throws `ArgumentException`), same defense-in-depth pattern used elsewhere in this codebase. The UI field is labeled exactly "Payment Date" with a caption noting it's the recurring payment day of the month, per explicit user wording.
  - **Anchor-preserving date math**: `GetNextBillingDate` always computes `PaymentDate.AddMonths(cycles * periodMonths)` from the *original* `PaymentDate`, never by repeatedly adding a period to the previous cycle's result — this avoids permanent day-of-month drift when `DateTime.AddMonths` clamps for a short month (e.g. a Jan 31 anchor must land on Mar 31 the next month, not get stuck at Feb 28's clamped value forever after). Verified via a throwaway console harness (`ProjectReference` to the real project, deleted after) covering a Jan-31 monthly anchor across Feb/Mar/Apr/May (confirms no permanent drift), a quarterly anchor, and a Feb-29 annual anchor across leap and non-leap years (confirms it returns to Feb 29 on the next leap year rather than drifting to the 28th permanently) — all matched hand-calculated expected dates exactly.
  - **Migration** (`AddStorageRentalPaymentDate`) follows the established safe-NOT-NULL pattern for existing rows: add nullable → `UPDATE StorageRentals SET PaymentDate = StartDate WHERE PaymentDate IS NULL` → alter to NOT NULL. Applied to the local dev DB; confirmed via direct SQL that all pre-existing seeded rentals backfilled correctly. Seed data (`SeedDummyDataAsync`) also sets `PaymentDate = startDate` for newly-seeded dummy rentals.
  - **Verified in the browser** (temporary `[Authorize]` removal on `StoragePropertyDetails.razor`, restored after): the "Payment Date" field and its caption render correctly in Start Rental; picking a date more than one month past the start date is rejected client-side with the exact validation message; the boundary value (`StartDate + 1 month` exactly) is accepted and the save proceeds as far as the (expected, since nothing is logged in during this unauthenticated check) "unable to determine current user" error — confirmed via direct SQL that no rental row was actually created by this test.
  - Committed and pushed to production on 2026-09-05.
  - **Not yet decided**: whether to surface `GetNextBillingDate` anywhere in the UI (e.g. a "Next Payment Due" column on `StoragePropertyDetails.razor`'s unit table). The field is captured and the calculation is verified correct, but nothing displays it yet — a natural follow-up once the user wants it.

- **Articles feature added (2026-09-06)**: a new content feature, planned via an explicit design discussion (audiences discussed: pre-login Home page, any registered user, RV/Boat Storage customers specifically) followed by plan mode. New `Data/Article.cs` (title, tag line, Markdown body, author, status, published date, audience, home-page-feature flag) implements the existing `ISupportImageAttachments` interface (a new nullable `ArticleId` owner FK added to `ImageAttachment`, same pattern as `CollectableId`/`StoragePropertyId`). New `"Article Publisher"` role (seeded alongside the existing three, via the same `EnsureRoleExistsAsync` helper) — `Admin` remains a superset. Authoring is dialog-style (`Components/Pages/Articles/ArticleDialog.razor`), following `StoragePropertyDialog.razor`'s `@ref`/`ShowModal`/`EditForm id=...` conventions exactly, including its image-upload block reused verbatim. `Services/IArticleService.cs`/`ArticleService.cs` mirrors `StorageService`'s shape.
  - **GUID-PK correction found during planning**: the user asked for GUID primary keys on new entities "wherever possible," reasoning it prevents ID-guessing/enumeration on public routes. Initial assumption (stated to the user, then corrected before implementation) that every non-Identity entity in the app uses `int` identity columns was **wrong** — `Collectable`/`CollectableCollection`/`CollectableCollectionItem` already use `Guid` PKs, via a simple `public Guid Id { get; set; } = Guid.NewGuid();` property-initializer default with **no** special EF `OnModelCreating` configuration needed (confirmed via the model snapshot: EF infers `ValueGeneratedOnAdd()` by convention for a `Guid` key, and since the C# default already sets a non-empty value before `SaveChanges`, EF just persists it as-is). `Article.Id` reuses this exact pattern. Scope was explicitly limited to new entities only — existing `int`-keyed tables (`StorageProperty`, `StorageUnit`, `StorageRental`, etc.) were left untouched; retrofitting those was discussed and deliberately deferred as its own much larger migration project, not bundled into this feature.
  - **Audience model**: `ArticleAudience` (`Public`/`RegisteredUsers`/`StorageCustomers`) gates *access* (enforced server-side in `ArticleService.GetVisibleArticlesAsync`/`GetPublishedArticleAsync`, never just hidden client-side) — kept deliberately separate from `FeatureOnHomePage`/`FeatureOnStorageLanding`, which are *placement* flags (do or don't show in a specific featured slot). An article can be `Audience=StorageCustomers` and featured nowhere, or `Audience=Public` and featured on Home. `FeatureOnStorageLanding` is modeled and query-ready (`IArticleService.GetFeaturedForStorageLandingAsync`) but **not called from any page yet** — there is no public-facing Storage landing page at all today (only `/storage/admin/*`, all admin-gated); wiring the display in is Storage Phase 2 work, deliberately not built here to avoid getting ahead of an unplanned page. New `Services/ArticleAudienceResolver.cs` computes a viewer's allowed-audiences list once (Public always; +RegisteredUsers if authenticated; +StorageCustomers if in that role, checked via `ClaimsPrincipal.IsInRole`, same as `Home.razor`'s existing Storage-Admin redirect check) — shared by `ArticlesIndex.razor` and `ArticleDetails.razor` so the gating logic isn't duplicated.
  - **Markdown + sanitization**: new NuGet packages `Markdig` (1.3.2) and `HtmlSanitizer` (9.2.1039, Ganss.Xss) — first content-rendering dependencies in the app. `Services/ArticleMarkdown.cs` is a plain static helper (`ToSafeHtml(string markdown)`), not DI-registered, since it's a pure function with no injected dependencies (unlike `ImageService` etc., which hold real state). Sanitizes at **render time**, not at save time, so a future sanitizer rule update applies retroactively to already-stored content. This matters because article bodies render on genuinely anonymous-reachable pages — verified by authoring an article with a raw `<script>alert(...)</script>` tag embedded in the Markdown and confirming via `document.body.innerHTML` in the live browser that the tag was fully stripped from the DOM (not merely visually hidden or inert-but-present), while the surrounding Markdown (headings, bold, links, lists) rendered correctly as real HTML elements.
  - **View counting extended to articles**: `HitCounter` (`Data/HitCounter.cs`) gained a nullable `ArticleId` FK (`DeleteBehavior.Cascade` — no prior precedent tied `HitCounter` to an owning entity, this is the first) plus a filtered unique index (`WHERE [ArticleId] IS NOT NULL`, same technique as `ApplicationUser.DisplayName`'s existing filtered-unique index) so at most one counter row exists per article while ordinary page-path counters are unaffected. `Components/HitCounter.razor` gained an optional `ArticleId` parameter; when set, it looks up/creates by `ArticleId` instead of `PagePath` (and still stores a derived `PagePath` of `/articles/{id}` for the required, non-nullable column). Verified in the browser: view count incremented correctly across repeat visits to an article, and — as a regression check — `Home.razor`'s pre-existing `<HitCounter PagePath="/" />` usage was confirmed still incrementing correctly, unaffected by the branch added for the new parameter.
  - **`ImageAttachment`'s "exactly one owner" CHECK constraint generalized**: the prior pairwise `CK_ImageAttachment_ExactlyOneOwner` expression (`(A IS NOT NULL AND B IS NULL) OR (A IS NULL AND B IS NOT NULL)`) doesn't scale past two owners, so it was rewritten as a sum-of-`CASE WHEN` form (`(CASE WHEN A IS NOT NULL THEN 1 ELSE 0 END + CASE WHEN B ... + CASE WHEN C ...) = 1`) — reusable indefinitely as more owner types are added. Verified directly via SQL: a literal insert with two owner columns set was rejected by the constraint, a literal insert with zero owner columns set was rejected, and a valid single-owner insert succeeded (an earlier attempt at this test gave a false "succeeded" reading because its `INSERT ... SELECT` source table happened to have zero rows locally — worth remembering that an `INSERT...SELECT` against an empty source silently inserts nothing rather than erroring, which can look like a passed test when it wasn't actually exercised).
  - **Real bug found and fixed during verification**: `Articles.razor`'s Publish/Unpublish button handlers originally passed `GetCurrentUserIdAsync()`'s result straight through without checking for an empty string first — unlike `ArticleDialog.razor`'s `HandleSave`, which already had this guard (matching the established defensive pattern documented earlier for the Storage dialogs). Passing an empty string as `ModifiedByUserId` would violate the FK constraint at the database level with a raw SQL exception surfaced to the user, exactly the same class of bug fixed previously in the Storage dialogs. Fixed by adding the same `string.IsNullOrEmpty(currentUserId)` guard (with a friendly Snackbar message) to both handlers before this shipped, for consistency with the rest of the codebase — not something the user asked for directly, but the same standing convention this project has already established twice.
  - Migration `AddArticles` applied cleanly to the local dev DB (additive only — new table, two new nullable FK columns, rewritten check constraint; no backfill needed).
  - **Verified end-to-end in the browser** using the same temporary-`[Authorize]`-removal technique established throughout this project (never logging in as the real seeded Admin): the authoring dialog (title/tagline/unrestricted-author-autocomplete/status/audience/feature-toggle/Markdown body/image upload) renders and binds correctly; a `RegisteredUsers`-audience article was correctly excluded from both the anonymous `/articles` list and directly-linked `/articles/{id}` (shown a generic "not found" message that doesn't distinguish nonexistent/unpublished/audience-restricted, avoiding information disclosure); a `Public` + `FeatureOnHomePage` article correctly appeared in Home's new "Featured Articles" section. `[Authorize]` was restored immediately after and reconfirmed via `curl` that `/articles/admin` correctly 302s to login for anonymous requests while `/articles` (public) still returns 200, and that the pre-existing `/storage/admin` gating is unaffected. All test article rows and their hit-counter rows were deleted from the dev DB afterward.
  - Committed and pushed to production on 2026-09-06.
  - **Deliberately deferred** (per explicit discussion with the user before implementation): sending articles as styled emails. `ArticleAudience` already tells you who'd receive one, so this can be added later without a schema change, but no `IsEmailable` flag or send flow exists yet.
- **Two real bugs found and fixed (2026-09-06), reported by the user after adding a real article with a photo in the dev DB**:
  1. **Missing image on Home's featured cards**: `Home.razor`'s "Featured Articles" section never rendered `article.GetMainImage()` at all — a plain oversight, since `Articles.razor` and `ArticlesIndex.razor` both correctly use it. Fixed by adding the same image-with-`srcset` block used on those pages.
  2. **`System.InvalidOperationException: A second operation was started on this context instance before a previous operation completed`**, an unhandled circuit-crashing exception on `Home.razor`, thrown from inside `ArticleService.GetFeaturedForHomeAsync`. **Root cause**: Blazor Server keeps one DI scope — and therefore one scoped `ApplicationDbContext` instance — alive for a circuit's *entire session*, not per page render, since in-app navigation via the router doesn't create a new scope. `ArticleService` was constructed with that single shared, injected `ApplicationDbContext` and used it for every method; EF Core's `DbContext` isn't safe for concurrent use, so a still-in-flight query from whatever page the user had just been on (e.g., `Articles.razor` reloading its list after a save) could race a query the next page fires immediately on navigating away, if the user navigated before the previous query finished. This risk existed structurally the moment any two DbContext-touching operations could overlap in one circuit; it had no way to surface before because `Home.razor` never did its own DB work until the "Featured Articles" section was added, so there'd never been anything for a leftover query to collide with there.
     - **Fix**: `Program.cs` now registers `AddDbContextFactory<ApplicationDbContext>` instead of `AddDbContext<ApplicationDbContext>`, with the plain scoped `ApplicationDbContext` (every existing `@inject ApplicationDbContext`/constructor-injected consumer, unchanged) derived from that same factory via `AddScoped(sp => sp.GetRequiredService<IDbContextFactory<ApplicationDbContext>>().CreateDbContext())` — registering both `AddDbContext` and `AddDbContextFactory` directly side by side fails DI validation at startup (`Cannot consume scoped service ... from singleton IDbContextFactory`), so the factory has to be the single source of truth. `ArticleService` was rewritten to take `IDbContextFactory<ApplicationDbContext>` and create a fresh `await using var context = await _contextFactory.CreateDbContextAsync();` **per method call** instead of holding one context for its whole lifetime — this makes the specific race structurally impossible (no two `ArticleService` calls can ever share a mutable context instance) rather than just less likely.
     - **Scope note**: only `ArticleService` was converted. `StorageService`, `CollectableService`, `ImageService`, `IrcEventService`, and `HitCounter.razor`'s direct `ApplicationDbContext` injection still share the one circuit-scoped instance — the same theoretical risk technically still exists between any two of *those*, unchanged from before this session and not something introduced by Articles. Converting them too would be a much larger, separate refactor; deliberately not done here since nothing reported a problem there and it's not part of what was asked.
     - **Verified**: clean app startup after the DI fix (the initial `AddDbContextFactory` + `AddDbContext` combination failed fast with a clear validation error, caught immediately via `dotnet run`). Reproduced the general shared-circuit-navigation scenario via 15 rapid alternating in-app nav clicks between `/articles` and `/` (via `element.click()` on the actual `MudNavLink`s, not raw browser navigation — a hard `navigate()` tears down and recreates the circuit each time and would never have exercised this at all) with no exceptions in the server log afterward. The original crash itself was never independently re-triggered on demand (timing-dependent by nature), but the fix is structural, not probabilistic, so this is expected.
  - Committed and pushed to production on 2026-09-06.
- **Real `srcset` sizing bug found and fixed (2026-09-06/07), from the user asking to double-check that article cards actually select the right image size**: every responsive `<img>` in the app (Articles' 3 spots, plus the pre-existing `StorageProperties.razor` it was copied from) hardcoded `srcset="{thumb} 400w, {medium} 1000w"`, but `ImageService.ResizeAndEncodeJpeg` caps the **longer** dimension at 400/1000/2048px — for a portrait photo, the actual generated *width* comes out smaller than the label claims (verified on the user's own uploaded 1350x1800 portrait photo: thumb was really 300px wide, medium really 750px, not 400/1000). Browsers trust a `srcset` width descriptor at face value when picking a candidate, so an inflated label isn't just a bandwidth inefficiency — it can make the browser pick a candidate believing it covers more display width than it actually has, upscaling/blurring it.
  - **Fix**: `ImageAttachment` gained nullable `ThumbnailWidth`/`MediumWidth` columns (migration `AddImageAttachmentVariantWidths`, purely additive) plus `[NotMapped] EffectiveThumbnailWidth`/`EffectiveMediumWidth` computed properties that fall back to the historical 400/1000 constants when null (existing rows saved before this change). `ImageService.ResizeAndEncodeJpeg` now returns the actual encoded width instead of `void`; `SaveFromStreamAsync`/`ImageUploadResult` carry it through. All four call sites that build an `ImageAttachment` from an `ImageUploadResult` (`ArticleService.AddArticleImageAsync`, `StorageService.AddPropertyImageAsync`, and the inline construction in `AddCollectable.razor`/`EditCollectable.razor`) now copy these fields. Every `srcset` in the app (all 5 locations) now interpolates `image.EffectiveThumbnailWidth`/`EffectiveMediumWidth` instead of the literals `400`/`1000`.
  - **Verified thoroughly, not just by inspection**: (1) a throwaway console harness (real portrait JPEG through the actual `ImageService.SaveImageAsync`, `ProjectReference`-to-the-real-project technique already established in this session) confirmed the recorded `ThumbnailWidth`/`MediumWidth` (300/750) exactly match the real on-disk pixel widths decoded independently via SkiaSharp. (2) A full real-browser round trip — upload via the actual `ArticleDialog` UI (using the `file_upload` browser tool against the real `<InputFile>`, not a synthetic bypass) with a temporary hardcoded-current-user-id swap in `GetCurrentUserIdAsync` (reverted immediately after, never touched the real login form) — confirmed the saved DB row carried the correct real widths (300/750), and reading `img.srcset` directly from the live rendered admin card showed the corrected values verbatim. (3) Confirmed the fallback path too: the pre-existing test article's original image (saved before this fix, `ThumbnailWidth`/`MediumWidth` still `NULL`) continued rendering with `400w`/`1000w` with no error - backward compatible. All test rows, uploaded test files, and the temporary code changes were removed/reverted afterward.
  - **Aside confirmed while investigating**: fine-grained `srcset` breakpoint testing via nested same-origin iframes (previously a trusted technique in this project for CSS breakpoint checks) gave inconsistent/contradictory results here and was abandoned in favor of a clean isolated test page plus the real end-to-end browser check above — worth remembering that the iframe technique's reliability may not extend to `sizes`/`srcset` resource-selection behavior specifically, only to CSS layout/breakpoints.
  - Committed and pushed to production on 2026-09-06.
- **Upgraded to .NET 10 (2026-09-07)**: driven by .NET 8's end of support landing November 10, 2026 (~2 months out at the time) and the user's web host newly supporting .NET 10 hosting. `TargetFramework` → `net10.0`; every `Microsoft.EntityFrameworkCore.*`/`Microsoft.AspNetCore.Components.WebAssembly*`/`Microsoft.AspNetCore.Diagnostics.EntityFrameworkCore`/`Microsoft.AspNetCore.Identity.EntityFrameworkCore` package bumped from `8.0.7` to the current stable `10.0.11` line via `dotnet add package` (resolves true-latest automatically rather than a possibly-stale hardcoded version). `MudBlazor` deliberately stayed at `6.21.0` — upgrading it to the .NET-10-supporting `9.x` line means crossing three major versions with real breaking changes each, agreed with the user as a separate, later effort; this pass confirmed 6.21.0 keeps working correctly on net10.0 as-is (verified interactively in the browser, not just "it compiles" — see below).
  - **`Microsoft.EntityFrameworkCore.Sqlite` removed entirely** (not just bumped) — grepping the actual `.cs` source confirmed it was never used anywhere (the app exclusively uses `UseSqlServer`; this was vestigial scaffolding), and it was the package dragging in a NuGet-flagged high-severity vulnerable transitive dependency (`SQLitePCLRaw.lib.e_sqlite3` 2.1.6). Removing unused-and-vulnerable beats upgrading unused-and-vulnerable.
  - **Real bug found and fixed, not just a version-bump formality**: after the upgrade, the app started cleanly and pages loaded, but *nothing interactive worked at all* — `HitCounter` stuck forever on "Loading...", the hamburger menu did nothing. Root cause, found via `performance.getEntriesByType('resource')` and a direct `fetch()` in the live browser (not guessed): `/_framework/blazor.web.js` was returning a **404**, so `window.Blazor` never existed and no SignalR circuit could ever connect — every `@rendermode="InteractiveServer"` component was permanently inert, though static/pre-rendered content looked fine, which is what made it easy to miss with a quick glance. Cause: `Program.cs` still used the old .NET 8-template `app.UseStaticFiles()`; since .NET 9, Blazor Web Apps need `app.MapStaticAssets()` instead — it's the endpoint-routing-integrated replacement, and without it the framework's own combined Server+WebAssembly `blazor.web.js` isn't mapped. This also explained a startup warning already visible before the fix ("Mapped static asset endpoints not found. Ensure 'MapStaticAssets' is called before 'AddInteractiveWebAssemblyRenderMode'") that would've been easy to dismiss as cosmetic. Fixed by swapping `app.UseStaticFiles()` → `app.MapStaticAssets()` in `Program.cs`. **This is the one genuinely load-bearing code change in this whole upgrade** — everything else was version numbers.
  - `.vscode/launch.json`'s hardcoded debug-output path updated `net8.0` → `net10.0` (same class of path-drift bug already fixed once before for this exact file, noted earlier in this checkpoint under "Recent Decisions").
  - **New warnings surfaced by the newer analyzer/SDK, left alone as out of scope**: 13 `BL0008` warnings across the scaffolded `/Account` Identity pages (`[SupplyParameterFromForm]` property with a property initializer, a long-standing scaffolded-template pattern, not something this upgrade changed) — real, but pre-existing risk unrelated to the .NET 10 move itself, not touched here.
  - **Verified thoroughly**: `dotnet build` clean (0 errors); `dotnet ef database update` confirmed the full existing migration history applies cleanly under EF Core 10 tooling ("already up to date", no errors); clean app startup with migrations/seeding; `curl` confirmed `/`, `/articles` return 200 and `/articles/admin`, `/storage/admin` correctly 302 to login anonymously (Identity cookie auth intact under the bumped Identity package). **Real browser verification, not just HTTP checks**: confirmed `window.Blazor` becomes defined and `_framework/blazor.web.js` returns 200 after the `MapStaticAssets` fix; confirmed `HitCounter` resolves to a real number instead of hanging on "Loading..."; opened the `/articles/admin` "Add Article" `MudDialog` via the established temporary-`[Authorize]`-removal technique (reverted immediately after) and confirmed the autocomplete, selects, toggle, and file input all render and behave correctly — the real test of whether MudBlazor 6.21.0 still works on net10.0, which it does.
  - **Manual action still needed from the user, outside what Claude can do**: the production Windows server needs the **.NET 10 ASP.NET Core Hosting Bundle** installed before/with the next deploy — this is a framework-dependent deployment (not self-contained), so the matching shared runtime must be physically present on the host; the existing .NET 8 hosting bundle won't satisfy a net10.0 app. No access to that machine from this session to verify or install it.
  - Committed. User confirmed the hosting bundle is now installed on the production server (2026-09-07) — see the MudBlazor upgrade entry immediately below, which was gated on this. Pushed to production (`07388f5`).
- **Upgraded MudBlazor 6.21.0 → 9.9.0 (2026-09-07)**, crossing three major versions (7, 8, 9), each with its own real breaking-change list — done incrementally (bump → build → fix → browser-verify → commit, per version) rather than a direct jump, per MudBlazor's own recommended migration path. Three separate local commits (`70ba2c4` v7, `46f72ba` v8, `fdc315d` v9), all pushed to production.
  - **v7 (6.21.0→7.16.0)**: `@bind-IsVisible`/`IsVisibleChanged` on `MudDialog` renamed to `@bind-Visible`/`VisibleChanged` app-wide (9 dialog components); `MudList`/`MudListItem` became generic, needing explicit `T="string"` even when unbound, and `Clickable`/`DisablePadding` were replaced by `ReadOnly`/`Padding` (removed outright since the new defaults already matched); `PaletteDark.BackgroundGrey` → `BackgroundGray` (and the CSS custom property `--mud-palette-background-grey` → `-gray`, `sed`-renamed across 5 files); `MudAutocomplete.SearchFunc` gained a `CancellationToken` parameter (`ArticleDialog.razor`'s and `StorageRentalDialog.razor`'s author/customer search methods updated). Also caught and removed a dead `AlignItems` attribute on `MudGrid` in 3 files — never a real parameter, silently swallowed by attribute splatting since v6, surfaced by MudBlazor's new v7 compile-time analyzer (MUD0001/MUD0002), which is a genuinely useful safety net this migration relied on throughout — it catches renamed/removed Razor attributes that a plain `dotnet build` alone would not.
  - **v8 (7.16.0→8.15.0)**: one real code fix, `SnackbarOptions.Onclick` → `OnClick` in `AddCollectable.razor`. Hit a false-alarm bug during verification worth remembering for future upgrades: after the bump, every `MudDialog` containing a `MudTextField`/`MudInput` silently failed to open, with the server log showing `JSException: 'mudElementRef.addOnBlurEvent' is not a function`. Root-caused (via direct prototype inspection of `window.mudElementRef` against the real installed package's JS source, not guessed) to a **stale browser HTTP disk-cache** of the pre-upgrade `MudBlazor.min.js` — the server was serving the correct 8.15.0 file the whole time. A manual `fetch(cache:'no-store')` patch fixed the global object's shape but not the actual dialog behavior (Blazor's JS interop dispatcher had its own separately-captured stale reference); a genuine hard reload (`ctrl+shift+r`) was required to actually fix it. This is the exact same class of gotcha already hit and documented multiple times in this file for CSS (`link.href += '?v='+Date.now()`) — now confirmed to also apply to `<script src>` tags, not just stylesheets. Worth trying a hard reload FIRST the next time any post-upgrade MudBlazor JS-interop error shows up that looks inexplicable given the server is confirmed serving the right file.
  - **v9 (8.15.0→9.9.0)**: `StorageDashboard.razor`'s Donut and Bar `MudChart`s needed a real rewrite, not just renames — v9 unifies the whole chart data model. `InputData`/`InputLabels`/`XAxisLabels` → `ChartSeries`/`ChartLabels`; plain `ChartOptions` → type-specific `DonutChartOptions`/`BarChartOptions`; chart data is now `List<ChartSeries<double>>` instead of a raw `double[]`/`List<ChartSeries>`. Reflected directly against the installed 9.9.0 assembly (via a throwaway console harness `ProjectReference`-ing MudBlazor, same technique class as this project's other "verify against the real installed package" checks) rather than trusting MudBlazor's own migration-guide prose, which turned out to describe a `.AsChartDataSet()` extension method with a different actual signature than documented (it returns `List<ChartSeries<T>>`, not `List<ChartData<T>>`) — the real fix ended up simpler than the guide implied: `ChartData<T>` has an implicit conversion from `T[]`, so `Data = new double[] {...}` just works directly on a `ChartSeries<double>`. Both `<MudChart>` tags also needed an explicit `T="double"` since v9's generic `MudChart<T>` no longer infers its type argument automatically in this usage. Separately, `MudTabs.PanelClass` was renamed to `TabPanelsClass` (caught by the same MUD0002 analyzer that helped throughout this upgrade) — one usage in `EditUserDialog.razor`, backing the fixed-height Roles/General tab CSS from earlier work; confirmed in the browser that switching tabs still doesn't resize the dialog.
  - **Verified in the browser for all three steps** using the established temporary-`[Authorize]`-removal-and-restore technique (never the real Admin password): dialog open/close and text-field/autocomplete interaction (`ArticleDialog`, `EditUserDialog`), the Roles tab's `MudDropContainer` drag-and-drop (verified via native `DragEvent` dispatch through `javascript_tool`, since CDP's synthetic mouse `left_click_drag` doesn't trigger real HTML5 drag-and-drop — it just does a text selection instead; this is a testing-technique note worth remembering for any future drag-and-drop verification in this app), the Storage dashboard's charts (data and theme colors correct at every step), and the mobile drawer/app-bar z-index behavior (verified via the established same-origin-iframe-at-390×844 technique) — all held up unchanged across all three major version bumps. One small, non-functional v9 visual difference noticed and not treated as a bug: the Donut chart's legend now appears to omit categories with a current value of 0 (e.g. "Reserved" wasn't shown when 0 units were reserved), where v8/v7 always listed all 4 fixed category labels regardless of value — data and colors are otherwise identical.
  - `dotnet build` clean (0 errors, 0 MudBlazor-analyzer warnings) at every step; anonymous `curl` checks confirmed `/admin/allUsers`, `/storage/admin`, `/storage/admin/properties`, `/articles/admin` all correctly 302 to login after each step's `[Authorize]` attributes were restored.
  - Pushed to production (2026-09-07).
- **Production image-loading regression found, and a targeted-but-unconfirmed fix pushed (2026-09-07)**: after the .NET 10 + MudBlazor 9.x pushes above went to production, the user reported every uploaded image on the site had stopped loading, despite confirming via FTP that the files still existed on disk at their expected `/uploads/...` paths. Suspected root cause: the .NET 10 upgrade's `UseStaticFiles()` → `MapStaticAssets()` swap (documented above) — `MapStaticAssets()` serves only the build-time static-web-assets manifest, and files written to `wwwroot/uploads/` at *runtime* by `ImageService.SaveImageAsync` are never in that manifest.
  - **Not independently reproduced locally**: a runtime-written test file placed directly under `wwwroot/uploads/` after `dotnet build` was served correctly by `MapStaticAssets()` alone on plain Kestrel — both with and without `UseStaticFiles()` present, and a genuine 404 sanity check confirmed this wasn't a blanket catch-all. So the actual root cause is most likely something specific to the production IIS/Windows hosting environment (no `web.config` or publish profile is checked into this repo to inspect) that this Linux/Kestrel sandbox can't replicate.
  - **Fix applied anyway**: restored `app.UseStaticFiles()` alongside `app.MapStaticAssets()` in `Program.cs`, as cheap, low-risk insurance for whatever the real IIS-specific difference is — the code comment explicitly says this is not a confirmed root-cause fix, just a reasonable safety net. Committed and pushed (`ae1b342`).
  - **Still waiting on**: the user redeploying and confirming whether images come back. If not, the next step is getting the actual HTTP response (status code + headers) for a failing image request directly from production — that's the concrete diagnostic this investigation still needs and couldn't get from local testing alone.
- **Articles nav-link visibility, Home heading, and the sidebar drawer's two real layout bugs fixed (2026-09-07)**:
  - `NavMenu.razor`'s "Articles" link moved inside the `<AuthorizeView><Authorized>` block, per explicit request — no longer shown to anonymous visitors. (`/articles` itself is still a public page via direct link; only the nav-menu entry point was hidden.)
  - `Home.razor`'s "Featured Articles" heading renamed to "Latest News" and its bottom margin bumped `mb-3`→`mb-6` (12px→24px) to fix a real, previously-unnoticed visual bug: `MudGrid`'s default `Spacing="6"` gives it `margin-top: -24px` (its built-in gutter-alignment technique), and the heading's prior 12px margin wasn't enough to absorb that, so the grid rode up and visually clipped the last ~12px of the heading's own text — confirmed via `getBoundingClientRect()` before fixing (heading bottom vs. grid top overlapped by exactly 12px), not guessed. **General lesson**: any heading placed directly above a `MudGrid` needs at least as much bottom margin as the grid's `Spacing`-driven negative top margin, or this same clipping recurs — checked (via grep) that no other `MudGrid` in the app has a heading immediately preceding it, so this was an isolated case, not a systemic one.
  - **Sidebar/drawer investigated and fixed at the root, not rebuilt from scratch**: the user reported two concrete symptoms with `AppShell.razor`'s `MudDrawer` — closing it never let page content reclaim its space, and opening it after scrolling down showed no visible nav items — and asked whether the whole nav implementation should be redone from scratch, now that the app is on current MudBlazor. Both symptoms were root-caused precisely via direct DOM/CSS inspection in the browser, not guessed:
    1. **Drawer wasn't actually `position:fixed`**: `MudDrawer.Fixed` defaults to `true` (confirmed via reflection against the installed 9.9.0 assembly and a standalone `new MudDrawer()` instance) — which should mean `position:fixed`, anchored to the viewport — but the live DOM showed `position:absolute` and no `mud-drawer-fixed` class, both with and without explicitly setting `Fixed="true"` in the markup. Scrolling a real page 2000px down and re-checking the drawer's `getBoundingClientRect()` confirmed it scrolls away with the page content (`top` became -1936px) — exactly matching the reported "no visible options" symptom, since the drawer's actual nav items were rendering far above the visible viewport.
    2. **`MudLayout` is frozen at first paint and never reacts to the drawer's state again**: `MudLayout` is meant to toggle a `mud-drawer-open-responsive-md-left` class on itself whenever `MudDrawer`'s `Open` state changes, which is what drives `.mud-main-content`'s `margin-left` (confirmed via MudBlazor's own shipped CSS — that class is what gates the `margin-left: var(--mud-drawer-width-left)` rule). But in this app, `MudLayout`/`MudMainContent` render as part of `MainLayout.razor`'s plain **static** SSR pass (no `@rendermode` on the layout itself), while only `AppShell` (containing `MudAppBar`/`MudDrawer`) is pinned to its own separate `@rendermode="InteractiveServer"` island — so `MudLayout` computes that class exactly **once**, during the very first static prerender, and never re-renders again for the life of the page, no matter how many times the drawer is later opened or closed. Confirmed directly: `.mud-layout` correctly carried `mud-drawer-open-responsive-md-left` at first load (matching the `_drawerOpen = true` default), but toggling the drawer closed afterward left `.mud-main-content`'s computed `margin-left` stuck at `240px` — while `.mud-drawer`'s own class (`mud-drawer--open`/`--closed`) correctly flipped every time, since that element is part of the *same* reactive island as the toggle button.
    - **A "textbook" single-render-tree `MudLayout` was attempted and reverted**: tried moving `@rendermode="InteractiveServer"` onto `MainLayout.razor` itself, so `MudLayout`/`AppShell`/`MudMainContent` would all share one interactive tree the way MudBlazor's own official templates do it. This turned out to be fundamentally impossible in this app: the router passes the routed page's rendered content into the Layout's `Body` parameter as a `RenderFragment`, and `RenderFragment` parameters cannot cross a static→interactive rendermode boundary (`Cannot pass the parameter 'Body' to component 'MainLayout' with rendermode 'InteractiveServerRenderMode'... arbitrary code and cannot be serialized` — a real 500 on every single page, confirmed then immediately reverted). This app *needs* per-page, non-global render modes specifically because `/Account/*` pages require static SSR for their POST-based cookie-auth flows — so the official MudBlazor single-tree layout pattern doesn't map onto this app's mixed-rendering requirement, and the multi-island split (already documented earlier in this file, originally introduced to work around a v6-era popover bug) turns out to be structurally *necessary* here, not just a historical leftover worth undoing.
    - **Fix actually applied — CSS-only, works with the island split instead of against it**: two new rules in `wwwroot/app.css`: (1) `.mud-drawer { position: fixed !important; }`, unconditionally forcing the viewport-anchored behavior `Fixed="true"` should have produced on its own; (2) `.mud-drawer.mud-drawer--closed ~ .mud-main-content { margin-left: 0 !important; }`, a plain sibling-combinator rule that drives the margin collapse off `.mud-drawer`'s own *reliably-live* open/closed class instead of `MudLayout`'s frozen one — confirmed via the DOM that `.mud-drawer` and `.mud-main-content` are direct siblings under `.mud-layout`, so this needs zero cooperation from `MudLayout`. `AppShell.razor`'s `<MudDrawer>` also gained an explicit `Fixed="true"` (self-documenting, even though the CSS override is what's actually doing the work now).
    - **Verified thoroughly in the browser**: scrolled a real page 2000px and opened the drawer — nav items fully visible, correctly anchored at the top of the viewport (confirmed via screenshot, not just measurement). Toggled the drawer closed/open repeatedly at desktop width — `.mud-main-content`'s margin correctly flips `240px`↔`0px` and the page visibly reflows to match, screenshotted both states. Re-verified mobile behavior via the established same-origin-iframe-at-390×844 technique — the overlay/scrim, the existing app-bar z-index fix, and the closed-by-default initial mobile state are all unchanged; nothing regressed. Confirmed zero new browser console errors, `dotnet build` clean, and anonymous `curl` checks still correctly 302 on every admin route.
    - **Scope note**: this leaves the underlying multi-island render-mode architecture in place — confirmed structurally necessary for this app (not incidental), rather than something to remove. The fix works *with* that architecture via plain CSS instead of attempting a deeper rearchitecture the framework doesn't actually support here. If a future MudBlazor version fixes `Fixed="true"`'s class computation to work correctly without a live `MudLayout` ancestor, the `position: fixed` override becomes redundant-but-harmless; the margin-collapse override will likely always be needed for as long as this app keeps the island split.
  - Pushed to production (`16b7fba`).
- **All 14 `BL0008` warnings on the scaffolded `/Account` Identity pages fixed at the root, not suppressed (2026-09-07)**: these warnings (`Property '...Input' has [SupplyParameterFromForm] and a property initializer. This can be overwritten with null during form posts.`) exist because `[SupplyParameterFromForm]` doesn't merge posted form data into the existing `Input` object — on a POST, ASP.NET Core replaces the whole property with either a fresh instance built from the posted fields, or `null` if the POST doesn't include any of that property's fields at all. The `= new()` initializer only runs once at construction and is irrelevant to that overwrite, so it was actively misleading.
  - **Confirmed empirically, not just by reading the framework source, exactly where the risk bites**: crafted a raw `curl` POST to `/Account/Login` (valid antiforgery token + `_handler=login`, but omitting every `Input.*` field) against a build with the initializer removed but no guard yet — this returned a raw `EditForm requires either a Model parameter, or an EditContext parameter, please provide one of these.` exception. Critically, this proved the crash happens at **render time**, inside `EditForm`'s own `OnParametersSet` when `Model` is null — before the page's own submit handler could ever run. This ruled out "just guard inside the submit handler" as a real fix; the guard has to run before first render.
  - **Fix pattern applied uniformly across all 14 files** (`Login`, `Register`, `ForgotPassword`, `ResendEmailConfirmation`, `LoginWith2fa`, `LoginWithRecoveryCode`, `ExternalLogin`, `ResetPassword`, and `Manage/{ChangePassword,SetPassword,DeletePersonalData,Email,Index,EnableAuthenticator}`): (1) `[SupplyParameterFromForm] private InputModel Input { get; set; } = new();` → `private InputModel? Input { get; set; }` (nullable, no initializer — satisfies the analyzer honestly instead of suppressing it); (2) `Input ??= new();` as the first statement of `OnInitialized`/`OnInitializedAsync` (added one where none existed) — this runs *before* `EditForm` ever renders, so a malformed POST transparently falls back to an empty (validation will naturally reject it) instead of crashing; (3) the resulting `CS8602` "possibly null" warnings at each markup/code usage site resolved with the null-forgiving operator (`Input!.Email`) for simple single-line cases, or a real `if (Input is null) { ...; return; }` guard at the top of the submit handler for methods with multiple usages spanning a LINQ lambda (`Register.razor`'s and `Manage/Index.razor`'s display-name-uniqueness checks) — discovered empirically that `Input!.X`'s null-forgiving flow-narrowing does **not** survive out of a lambda closure back into the enclosing method's later statements, while a real `is null` check does, which is why those two files use the guard-clause form and the rest use `!`.
  - **Verified end-to-end, not just "it compiles"**: repeated the same malformed-POST `curl` technique (real antiforgery token, omitted `Input.*` fields) against `Login`, `Register`, and `ForgotPassword` post-fix — all three now return `200` and correctly re-render the normal form (confirmed via page content, e.g. "Create New Account" still present) instead of throwing. Also confirmed every page still renders correctly on a plain `GET` (two pages, `LoginWith2fa`/`LoginWithRecoveryCode`, correctly still 500/302 when hit out-of-sequence without a pending 2FA cookie — pre-existing, unrelated guard logic, not a regression). `dotnet build --no-incremental` confirmed clean: 0 `BL0008`, 0 `CS8602`, only 3 pre-existing unrelated `CS0414` (unused-field) warnings remain in `Home.razor`, untouched here since out of scope.
  - Pushed to production (`cc484e5`).
- **Real article-detail hero-image cropping bug found and fixed (2026-09-07)**, reported by the user as "the image looks different on mobile vs desktop before I push": `ArticleDetails.razor`'s main image used `object-fit: cover` with a fixed `max-height: 360px` and `width: 100%`. Since `cover` scales an image to fill *both* dimensions of its box and crops whichever axis overflows, and the box's width scales with viewport while its height stays pinned at 360px, the actual crop percentage is highly viewport-width-dependent for a portrait photo — confirmed by checking the real featured article's actual image dimensions in the dev DB and on disk (1350×1800, aspect ratio 0.75, same for all three generated variants): the math works out to ~69% of the image height visible on a 390px-wide mobile viewport vs. only ~30% visible in a ~900px-wide desktop `MudContainer`, which is exactly the "whole image on mobile, zoomed-in crop on desktop" difference reported.
  - **Fix**: switched to `object-fit: contain` inside a centered flex wrapper with the app's existing gray letterbox background (`var(--mud-palette-background-gray, ...)`, the same pattern already used for card-thumbnail image boxes elsewhere) — this shows the *entire* image on every viewport, letterboxed instead of cropped, so mobile and desktop now render identically (same visible content, just scaled). Deliberately scoped to `ArticleDetails.razor` only: the card-grid thumbnails on `Articles.razor`/`ArticlesIndex.razor`/`Home.razor` intentionally keep `object-fit: cover` in a fixed-size tile — cropping to a uniform tile is normal, expected behavior for a thumbnail grid, unlike a hero image presented as the article's actual content.
  - **Verified visually, not just by CSS reasoning**: loaded the real featured article in a normal desktop-width tab and in a same-origin 390×844 iframe (the project's established mobile-testing technique) side by side — both now show the identical framing of the photo, confirmed via screenshot. Also reloaded Home and the Articles list to confirm their unrelated card-thumbnail crops were untouched by this change.
  - Pushed to production (`4902de7`).
- **`MudGrid` negative-margin overlap bug found in 3 reported places, then swept across the whole app (2026-09-07)**: the user reported `MudGrid`'s built-in negative margin (`-24px` on `margin-top`/`margin-left` at the default `Spacing="6"`, confirmed directly from MudBlazor's shipped CSS — it never sets `margin-bottom`/`margin-right`) was making grid content overlap whatever sits directly above or to the left of it, on `Collectables`'s "All Collectables" view (a card poking past its `MudPaper`'s left edge), `MyCollectables` (the results grid covering the "Total Items"/"Total Value" row above it), and `Storage/Properties` (the results grid covering the bottom of the "Storage Properties" heading). This is the exact same root cause already fixed once for Home.razor's "Latest News" heading (see the 2026-09-06 entry above) — recurring because it depends on the *specific* margin/padding value chosen at each call site, not something a single global fix can catch.
  - **The precise mechanic, worth remembering**: when the negative-margin element (the grid) and the thing above it are two *different* elements, ordinary CSS margin-collapsing applies — a preceding `mb-N` and the grid's own unmodified `-24px` margin-top collapse to `N − 24`px (negative until N ≥ 24, i.e. `mb-6`). But when a utility class like `mt-N`/`mb-N` (all MudBlazor spacing utilities carry `!important`) is applied *directly to the grid itself* on the same property its own base rule sets, `!important` simply **replaces** the built-in `-24px` outright — it does not add to it. This distinction mattered in practice: `Admin.razor`'s second `<MudGrid Class="mt-4">` looked identical to the buggy cases but was never actually broken (`mt-4 !important` fully overrides the grid's own `-24px`, giving a flat `+16px` gap) — an initial "fix" changing it to `mt-6` was reverted after checking MudBlazor's raw CSS confirmed the original was already correct, rather than leaving an unexplainable, unnecessary diff in.
  - **Fixed by auditing every `<MudGrid` in the app** (not just the 3 reported), computing the real preceding-margin math for each, and bumping only the ones that were actually short of the 24px needed: `CollectionDetails.razor` (`pa-4`→`pa-6`, the padding-wrapper case — same math, `padding` instead of `margin`, since a MudGrid's negative margin pulls it outside its own parent's *padding* box, not just past a sibling), `MyCollectables.razor`, `Storage/Properties.razor` (both its heading *and* its own search-row grid), `AllUsers.razor`, `Articles.razor`, `ArticlesIndex.razor`, `Storage/Dashboard.razor`, `Admin.razor` (the heading instance only — see above), `EditCollectable.razor` (×2), `AddCollectable.razor`, `Storage/StoragePropertyDialog.razor` (×2), `Storage/StorageUnitDialog.razor`, `Articles/ArticleDialog.razor` (×2), `Manage/PersonalData.razor`, `Manage/EnableAuthenticator.razor` (a plain `<p>`, needed an explicit `mb-6` class since it has no existing utility class to bump), and `Manage/{Email,ChangePassword,Index,SetPassword}.razor` (whose actual predecessor is the *shared* `StatusMessage.razor`/`ConfirmDialog`-style component, conditionally rendered — fixed **once**, centrally, in `StatusMessage.razor`'s own `MudAlert` `mb-3`→`mb-6`, since it's reused by 23 files and a slightly bigger gap after it is always harmless).
  - **Verified precisely, not just "looks fine"**: for the two grid-to-grid cases, used `getBoundingClientRect()` to confirm the first grid's `bottom` and second grid's `top` are now pixel-identical (e.g. `MyCollectables`: both exactly `174.734375`) — an exact 0px gap, not just "no longer visibly overlapping." For the padding-wrapper case, confirmed via `getBoundingClientRect()` that the card's `left`/`top` now exactly equal the `MudPaper`'s own `left`/`top` (`525 === 525`) rather than being negative relative to it. Used the project's established temporary-`[Authorize]`-removal technique for the auth-gated pages, plus one throwaway test `Collectable` (linked into the real "All Collectables" system collection) since the dev DB had zero collectables to view — all test rows deleted afterward.
- **New `Components/ResponsiveImage.razor` component + full migration (2026-09-07)**, from the user's suggestion to stop hand-rolling the `srcset`/`sizes` pattern at every image display site — see the new "Reusable Components" section near the top of this file for the full parameter reference; read that before touching any image display code. In short: wraps the `Image.ThumbnailUrl`/`MediumUrl`/`EffectiveThumbnailWidth`/`EffectiveMediumWidth` → `<img srcset sizes>` pattern that had been hand-copied into 5+ files (with real bugs each time it was copied — see the 2026-09-06 `srcset` width-accuracy fix and the 2026-09-07 hero-image cropping fix above), plus a `Fit`/`FixedHeight` pair of parameters covering every combination actually used in the app (uniform cropped grid tiles, a single shrink-to-fit hero image, and a fixed-height *non*-cropped tile for `EditCollectable`'s zoomable "Current Images" grid).
  - **Migrated 10 call sites** to the new component: `Home.razor`, `Articles.razor`, `ArticlesIndex.razor`, `Storage/Properties.razor`, `ArticleDetails.razor` (today's earlier hero-image fix, now expressed through the component instead of hand-rolled inline), `MyCollectables.razor`, `CollectionDetails.razor`, `EditCollectable.razor`'s "Current Images" grid, and the "existing images" grids in `Articles/ArticleDialog.razor` and `Storage/StoragePropertyDialog.razor`. Two of these (`MyCollectables`, `CollectionDetails`) previously had **no `srcset` at all** — a genuine feature gain from the consolidation, not just a refactor — and both were also quietly switched from an ad-hoc inline "first image flagged `IsMainImage`, else `.First()`" lookup to the already-existing `GetMainImage()` extension method (correct fallback behavior when no image is flagged main; was already used everywhere else in the app).
  - **Deliberately left un-migrated** (documented in the Reusable Components section so this isn't rediscovered as an oversight): `ImageModal.razor`'s own full-size lightbox `<img>`, `AppShell.razor`'s static logo, the newly-selected-but-not-yet-uploaded blob-URL preview grids in `AddCollectable`/`EditCollectable`, and `CollectionDetails.razor`'s 60×60 avatar thumbnail in the "Add Collectable" picker list (too small to need responsive sizing, and would need a `Width` parameter the component doesn't have yet).
  - **Verified each migrated call site in the browser**, not just via a clean build: confirmed `srcset`/`sizes` render correctly via `img.srcset`/`img.sizes` on Home; confirmed the click-to-zoom `OnClick` parameter genuinely opens `ImageModal` on `EditCollectable`'s Current Images grid (screenshotted the lightbox); confirmed the overlay "remove" button in `ArticleDialog`'s existing-images grid still positions correctly over the new component (it renders a plain in-flow box, so the existing `position: relative` wrapper + absolutely-positioned button pattern needed no changes). Used the same temporary-`[Authorize]`-removal + throwaway-test-data technique as the `MudGrid` sweep above (a second throwaway `Collectable`, this time with a real `ImageAttachment` row and copied test image files, all deleted/removed afterward).
  - `dotnet build --no-incremental` clean: 0 errors, 0 warnings, across both this and the `MudGrid` sweep above.
  - Pushed to production (2026-09-07).
- **Email debugging + branded template infrastructure, first pass (2026-09-07)**: the user wants to be able to actually see outgoing emails while developing on this Linux machine (previously impossible - `EmailSettings` pointed at a non-existent `localhost:587`), and wants the plain, one-off HTML strings the three built-in Identity emails used (confirm email, reset-password link, reset-password code) replaced with a professional, reusable template system ahead of a future Storage-Customer signup flow.
  - **Mailpit** set up as the local SMTP catcher (see the Linux Dev Environment Setup entry above for the exact commands) - no app code changes needed for this half, since `SmtpEmailSender` was already a plain SMTP client; it just needed somewhere real to talk to.
  - **New `Components/Emails/` + `Services/EmailTemplateRenderer.cs`**: rather than pull in a third-party templating library, emails are just Razor components rendered to an HTML string via ASP.NET Core's own `HtmlRenderer` (`Microsoft.AspNetCore.Components.Web.HtmlRenderer`, built into .NET 8+ - the same mechanism used for static prerendering, confirmed via reflection against the installed 10.0.11 assembly rather than guessing the API shape). `EmailTemplateRenderer.RenderAsync<TComponent>(Dictionary<string,object?>)` creates a fresh DI scope + `HtmlRenderer` per call (the documented pattern for rendering outside a normal HTTP request - keeps concurrent sends from ever sharing renderer state) and is registered as a `Singleton` in `Program.cs` (cheap to hold, since it only stores an `IServiceScopeFactory`).
  - **`EmailLayout.razor`** is the shared branded chrome (blue header banner, white card, gray footer, `#1f6feb` accent matching `AppMudTheme.cs`'s `Primary` color) plus **`EmailButton.razor`** for a consistent call-to-action button - both written as old-fashioned table-based markup with inline styles throughout, deliberately *not* the CSS-classes/flexbox this app uses everywhere else, since most email clients (Outlook desktop especially) strip `<style>` blocks and don't support flexbox/grid at all. `ConfirmEmailTemplate.razor`/`ResetPasswordLinkTemplate.razor`/`ResetPasswordCodeTemplate.razor` are the three specific bodies, each just wrapping `EmailLayout`.
  - **`IdentitySmtpEmailSender.cs`** now calls the renderer instead of building strings inline. Every existing call site (`Register`, `ExternalLogin`, `ResendEmailConfirmation`, `ForgotPassword`, `Manage/Email.razor`'s change-email flow) goes through this one shared `IEmailSender<ApplicationUser>` implementation, so all of them picked up the new templates with zero per-page changes.
  - **Verified for real, not just compiled**: triggered actual `ForgotPassword` and `ResendEmailConfirmation` POSTs (the same real-antiforgery-token `curl` technique used throughout this project) against the real seeded Admin account and confirmed both emails arrived in Mailpit correctly branded, with a working reset link and confirmation link respectively (screenshotted both). The third template (reset-code, only reachable mid-2FA-login) was verified by calling `EmailTemplateRenderer` directly from a throwaway console harness (`ProjectReference`-to-the-real-project, deleted after) since triggering a full 2FA flow wasn't worth the setup for one already-proven code path. Mailpit's own built-in cross-client HTML-compatibility checker scored the rendered email at 90% support - the shortfall is exactly the kind of thing real HTML-email guides call out as an accepted Outlook-desktop-only limitation (`border-radius`, some `margin`/`padding` cases), not something worth chasing with VML fallbacks for a personal app's account emails.
  - **Not yet done**: no template exists yet for a Storage-Customer welcome/signup email, since that signup page itself doesn't exist yet (Phase 2, still not started) - this pass was explicitly scoped to re-platforming the three emails that already exist onto reusable infrastructure, ready for that page to add its own template the same way.
  - Pushed to production (2026-09-07).
- **Critical regression from the email-template work above, found and fixed same-day (2026-09-07)**: the user registered a real new account in production, clicked "Confirm Email" in the email, landed back on the site with no error - but the account was never actually confirmed. Root-caused precisely, not guessed: every call site building a confirmation/reset link (`Register`, `ExternalLogin`, `ResendEmailConfirmation`, `ForgotPassword`, both flows in `Manage/Email.razor` - 6 sites total) wraps the URL in `HtmlEncoder.Default.Encode(...)` before handing it to `SendConfirmationLinkAsync`/`SendPasswordResetLinkAsync`. That encoding was **necessary** when `IdentitySmtpEmailSender` built raw HTML strings via C# interpolation (Razor never touched the URL, so nothing else would encode it) - but now that the email body is a real Razor component (`EmailButton.razor`'s `href="@Href"`), Blazor **automatically** HTML-encodes any string written into an attribute. The result was double-encoding: a link's `&` between query parameters became `&amp;amp;` in the sent HTML instead of `&amp;`. A browser decodes HTML entities exactly once when parsing an attribute, so the *actual* URL it navigated to still contained the literal text `&amp;` instead of a real `&` - meaning ASP.NET Core's query-string parser never split `code=...` out as its own parameter at all. `ConfirmEmail.razor` correctly finds `Code` is `null` under this condition and silently does `RedirectManager.RedirectTo("")` (see its `OnInitializedAsync`) - explaining exactly what the user saw: page loads fine, no error, nothing confirmed.
  - **Confirmed the exact mechanism with a real reproduction** before touching any code: built a realistic multi-param URL, ran it through `HtmlEncoder.Default.Encode` once (matching the pre-fix call sites) then through the actual `EmailTemplateRenderer`/`ConfirmEmailTemplate`, and compared the resulting `href` HTML source against the same render *without* the redundant pre-encoding - confirmed `&amp;amp;` vs. the correct single `&amp;`.
  - **Fix**: removed the now-redundant (and actively harmful) `HtmlEncoder.Default.Encode(...)` wrapping at all 6 call sites - they now pass the raw callback URL straight through, letting the Razor component's own automatic attribute-encoding do its job exactly once, correctly.
  - **Verified with the most rigorous check used in this project for this class of bug**: registered a genuinely new account through the real `/Account/Register` endpoint end-to-end, fetched the real confirmation email from Mailpit, decoded its HTML-entity-encoded `href` exactly the way a browser would, issued a real GET to that URL, and confirmed via a **direct database query** that `AspNetUsers.EmailConfirmed` flipped from `0` to `1` for that account - not just "the page said thank you," the actual persisted state changed. Also confirmed the single-query-param case (`ForgotPassword`/`ResetPassword`, which wasn't actually broken by this specific bug since there's no second `&`-joined parameter to corrupt, but shares the same fix) still correctly binds `Code` from the query string end-to-end. All test rows (`regressiontest_*@example.com`) and Mailpit messages cleaned up afterward.
  - **Lesson for any future email-template or HTML-generation work**: a value that was manually pre-encoded for the *old* raw-string-based rendering path becomes actively wrong once that path is replaced by something (Razor, or any other auto-escaping renderer) that already encodes correctly on its own - double-encoding bugs are silent (no exception, no visible error) and only surface as "the feature quietly doesn't work," which is exactly what happened here. Worth specifically re-checking for this pattern (a manual `HtmlEncoder`/`Uri.EscapeDataString`/etc. call feeding into newly-templated output) any time a hand-rolled string-building code path is migrated to a real templating mechanism.
  - Pushed to production (`f7bafbc`) — this fixed a live production bug affecting real new signups.
- **DbContext-concurrency hardening completed (2026-09-07)**: closed out the "Residual DbContext-concurrency risk" noted below by converting every remaining circuit-scoped `ApplicationDbContext` consumer to the same `IDbContextFactory<ApplicationDbContext>` pattern already proven for `ArticleService` - each method now creates its own short-lived `await using var context = await _contextFactory.CreateDbContextAsync();` instead of sharing one context for the whole Blazor Server circuit's lifetime. Converted: `CollectableService.cs`, `IrcEventService.cs`, `StorageService.cs`, `CollectionService.cs` (full rewrite each), plus `HitCounter.razor`, `MyCollectables.razor`, `AddCollectable.razor`, `EditCollectable.razor` (per-method `@inject IDbContextFactory<ApplicationDbContext>` swap). `Program.cs` needed no changes - the factory registration and the `AddScoped(sp => factory.CreateDbContext())` bridge for existing `@inject ApplicationDbContext` consumers already existed from the `ArticleService` work.
  - **The original "residual risk" list (below) was both incomplete and partially wrong**: `ImageService.cs` was listed but, on inspection, has zero `ApplicationDbContext` dependency at all (pure file I/O) - it never needed conversion. A broader grep (`grep -rln "@inject ApplicationDbContext\|ApplicationDbContext _context\|ApplicationDbContext context\b"`) surfaced four consumers missing from the original list entirely: `CollectionService.cs`, `MyCollectables.razor`, `AddCollectable.razor`, `EditCollectable.razor` - all genuinely at risk (used within long-lived circuits) and converted. `ImageAttachmentMigrationService.cs` also matched the grep but was deliberately left alone: it's resolved and run exactly once at startup, before Kestrel accepts requests, so it's structurally never exposed to the concurrent-circuit-access race this effort closes off.
  - **Design pattern for methods with multiple sequential `SaveChangesAsync()` calls** (e.g. insert-parent-then-insert-children, where the child insert needs the parent's generated key): create **one** context per method call and reuse it for every operation in that method body - the risk being eliminated is concurrent use *across* method calls/circuits, not sequential awaited use *within* one method. `StorageService.SeedDummyDataAsync` and `AddCollectable.razor`/`EditCollectable.razor`'s `HandleValidSubmit` all follow this.
  - **`EditCollectable.razor`'s `HandleValidSubmit` uses `context.Collectables.Update(collectable)` right before `SaveChangesAsync()`**, not `Attach()`-then-mutate - matches the existing idiom in `StorageService.UpdatePropertyAsync`/`CollectionService.UpdateCollectionAsync`. `Attach()`-then-mutate was tried first (relying on EF's snapshot-based change tracking to diff only actually-changed properties) but reverted in favor of `Update()`'s simpler, already-proven blunt-mark-everything-Modified approach, for consistency and lower risk given the full `Images` navigation graph involved.
  - **Real logic bug caught and fixed during the `EditCollectable.razor` conversion**: `RemoveImage()`'s "if this was the main image, promote the next one" step (`collectable.Images.First().IsMainImage = true;`) used to work only because the *old* shared, never-disposed context's automatic change tracking silently persisted that mutation. Once each method gets its own short-lived context, that mutation on an object the new context never loaded would be silently lost. Fixed by adding an explicit `context.ImageAttachments.Update(collectable.Images.First());` before the final `SaveChangesAsync()`. Verified in the browser: uploaded two images, used "Set as Main" to flip which one was main, then removed the (new) main image and confirmed via direct SQL that the remaining image's `IsMainImage` flipped to `1` and its file was correctly promoted - not just that the page didn't crash.
  - **Second real bug found and fixed, unrelated to the conversion itself**: verifying `MyCollectables.razor`'s `DeleteCollectable` surfaced a genuine **pre-existing** production bug (present before tonight, in both the old and new code - a straight `Remove()` + `SaveChangesAsync()` either way) - deleting any real collectable always failed with `SqlException: The DELETE statement conflicted with the REFERENCE constraint "FK_CollectableCollectionItems_Collectables_CollectableId"`. Root cause: `CollectableCollectionItem -> Collectable` is deliberately `DeleteBehavior.Restrict` in `ApplicationDbContext.cs` (a design choice, per its own comment), but nothing anywhere ever removed a collectable's `CollectableCollectionItems` membership rows before deleting it - and every collectable is a member of at least its owner's auto-created "All Collectables" system collection (see `CollectionService.EnsureAllCollectablesCollectionExistsAsync`/`AddCollectableToCollectionAsync`, called from `AddCollectable.razor`), so this affected every collectable, not an edge case. **Fix**: `DeleteCollectable` now queries and `RemoveRange`s the collectable's `CollectableCollectionItems` rows in the same context/transaction, immediately before removing the collectable itself.
  - **Verification, per file**: `CollectableService.cs` - build-verified, its only real consumer (`CollectionDetails.razor`'s Add-Collectable modal) loaded without error (no test data present at the time, so results weren't independently checked). `IrcEventService.cs` - real `curl POST /api/irc/events` end-to-end, confirmed the row persisted via direct SQL, then deleted it; `Chat.razor` confirmed to load correctly (temporary `[Authorize]` removal). `HitCounter.razor` - real page reloads on Home confirmed the view count incremented correctly across requests. `StorageService.cs` - temporary `[Authorize]` removal on `StorageDashboard.razor`/`StorageProperties.razor`/`StoragePropertyDetails.razor` confirmed the dashboard stat cards/charts, property grid, and a property's full unit/rental table all rendered correctly against the real seeded dummy data. `CollectionService.cs` - every method (`CreateCollectionAsync`, `GetUserCollectionsAsync`, `GetCollectionByIdAsync`, `UpdateCollectionAsync`, `EnsureAllCollectablesCollectionExistsAsync`, `GetAllCollectablesCollectionAsync`, `AddCollectableToCollectionAsync` including its dedupe check, `RemoveCollectableFromCollectionAsync`, `DeleteCollectionAsync` including its system-collection-is-protected check) exercised end-to-end via a throwaway console harness (`ProjectReference`-to-the-real-project, deleted after) against the real dev SQL Server, using a temporary collectable/collection created and cleaned up by the harness itself. `AddCollectable.razor`, `EditCollectable.razor`, `MyCollectables.razor` - full real browser pass (temporary `[Authorize]` removal + a hardcoded seeded-Admin-id fallback in `OnInitializedAsync`, both reverted immediately after, never touching the real login form): created a real collectable with an image through the actual `<InputFile>` upload path, edited its description and added a second image, used "Set as Main" and "Remove" on the images (catching the bug above), confirmed `MyCollectables.razor`'s list reflected the edit, then deleted it (catching the FK bug above) and confirmed via direct SQL/disk inspection that the collectable, its images, and its collection-membership rows were all fully gone, with no orphaned files on disk.
  - `[Authorize]` attributes were restored on all 8 pages touched across tonight's work and the two prior sessions' still-pending items (`EditCollectable.razor`, `MyCollectables.razor`, `AddCollectable.razor`, `CollectionDetails.razor`, `Chat.razor`, `StorageDashboard.razor`, `StorageProperties.razor`, `StoragePropertyDetails.razor`), and `curl` reconfirmed every one of them 302s to login for anonymous requests again while public pages (`/`, `/articles`) still return 200.
  - Pushed to production (`ce97d5a`).
- **Article media layout templates added (2026-09-08)**: the article detail page always showed exactly one image (the "main" one) regardless of how many photos were uploaded, even though multi-photo upload already worked. Per user request (having seen this pattern on other sites), authors can now pick from six layout templates - Hero, Split (text-wrap), Gallery, Masonry, Carousel, and Inline breaks - and control which photo lands in which position by reordering the upload list.
  - **No new "slot" concept needed**: `ImageAttachment.DisplayOrder` (already existed) *is* the slot index - position 0 is what single-image layouts (Hero/Split) use, and is also already what `GetMainImage()` picks for card thumbnails. New `Article.MediaLayout` enum (`Data/ArticleMediaLayout.cs`: `Hero`/`SplitTextWrap`/`Gallery`/`Masonry`/`Carousel`/`InlineBreaks`, string-converted, default `Hero` via `HasDefaultValue` - migration `AddArticleMediaLayout` is purely additive, so every pre-existing article keeps rendering exactly as before). New `IArticleService.ReorderArticleImagesAsync(articleId, orderedImageIds)` sets `DisplayOrder = index` and `IsMainImage = (index == 0)` together in one pass - the only place `IsMainImage` is reassigned after upload, so "first in order" and "main image" never diverge.
  - **`ArticleDialog.razor`** gained a "Media Layout" `MudSelect` (with a per-option one-line caption explaining what it does and which photos it uses) plus drag-and-drop reordering of already-uploaded photos, reusing the `MudDropContainer`/`MudDropZone` pattern from `EditUserDialog.razor`'s Roles tab - but as a **single** zone (pure reordering via `MudItemDropInfo<T>.IndexInZone`, confirmed present in the installed MudBlazor 9.9.0 assembly via reflection) rather than the two-zone assign/unassign shape used there. New `ArticleImageDropItem` wrapper class mirrors `RoleDropItem`'s convention; each tile shows a numbered slot badge. Reordering is deferred to Save (matching how photo removal already worked), by calling the new service method with the final `_existingImageItems` order.
  - **New `Components/Pages/Articles/ArticleMediaLayoutView.razor`** owns rendering the body *and* photos together (moved the `ArticleMarkdown.ToSafeHtml` call here from `ArticleDetails.razor`, since `InlineBreaks` needs the raw Markdown split into per-paragraph chunks rendered separately, while every other layout renders the whole body once). Per layout: Hero/Split use only the first image (`Article.GetMainImage()`) and sit at/near the top; Gallery (`MudGrid`)/Masonry (plain CSS `column-count`, no JS library)/Carousel (`MudCarousel<ImageAttachment>`, confirmed bundled in the installed MudBlazor package, no new NuGet dependency) all use every image and render *after* the body, per explicit user preference; Inline breaks splits the raw Markdown on blank-line paragraph boundaries into `imageCount + 1` buckets and interleaves a full-width image between them, capping usable images at `paragraphs - 1` so a short article with many photos doesn't produce pointless empty text segments. `ArticleDetails.razor` now just renders `<ArticleMediaLayoutView Article="_article" />` where the old hardcoded hero image + body div used to be.
  - **Preview cards deliberately unchanged**: `Articles.razor`, `ArticlesIndex.razor`, and Home's "Latest News" section keep using `GetMainImage()` for a single thumbnail regardless of an article's layout - standard practice for card previews, confirmed still true after this change.
  - **Verified end-to-end in the browser** (temporary `[Authorize]` removal on `Articles.razor` + a hardcoded seeded-Admin-id fallback in `ArticleDialog.razor`'s `GetCurrentUserIdAsync`, both reverted immediately after): created a real test article with 3 photos and a 3-paragraph body, then cycled it through all six layouts (switching and re-saving via the dialog each time) confirming each one visually - Hero banner, Split text-wrap (text visibly wraps around the floated image, confirmed via screenshot), Gallery grid, Masonry (confirmed `column-count:3` actually applied via `getComputedStyle`, though visually indistinguishable from Gallery here since all 3 test images share one aspect ratio), Carousel (clicked Next, confirmed the slide and bullet indicator both advanced), and Inline breaks (confirmed via screenshot that with 3 paragraphs only 2 of the 3 images were interleaved, matching the "cap at paragraphs - 1" design, not all 3 crammed in). Confirmed the real pre-existing "This is a test." article (created before this migration) still renders as Hero, byte-for-byte the same as before. Confirmed `/articles`, `/articles/admin`, and Home's cards still show only the single thumbnail for the multi-layout test article.
  - **Drag-and-drop reordering was not verified through actual browser interaction** - CDP's synthetic `left_click_drag` doesn't trigger real HTML5 drag-and-drop (already documented elsewhere in this file), and several rounds of manually dispatching native `DragEvent` sequences via `javascript_tool` (the technique that worked for `EditUserDialog`'s two-zone case) didn't reproduce a visible reorder here either, for reasons not fully root-caused. Instead, `ReorderArticleImagesAsync` itself was verified directly and thoroughly via a throwaway console harness (`ProjectReference`-to-the-real-project, deleted after) against the real persisted test images: reversed a 3-image order, confirmed `DisplayOrder`/`IsMainImage` updated exactly as expected (including exactly one `IsMainImage=true` at all times), then restored the original order and reconfirmed. The `ExistingImageDropped` handler code itself is a direct, minimal application of MudBlazor's documented single-zone-reorder pattern (remove the dropped item, reinsert at `IndexInZone`) - **worth a real manual drag test the next time this dialog is used with a signed-in session**, since it's the one piece of this feature not independently confirmed working end-to-end.
  - **Real, pre-existing bug noticed in passing, not fixed in this pass**: `Articles.razor`'s `HandleDeleteConfirmed` only calls `ArticleService.DeleteAsync` - the DB rows cascade-delete correctly, but the physical image files on disk are never cleaned up (unlike `MyCollectables.razor`'s delete flow, which does call `ImageService.DeleteImageAsync` per image first). Left as-is at the time since it was unrelated to the layout-templates feature; **fixed properly the same day, see the "Orphaned-photo cleanup made uniform" entry directly below**. Test files from tonight's verification were deleted from disk by hand.
  - `dotnet build --no-incremental` clean: 0 errors, 0 warnings. `[Authorize]` restored on `Articles.razor`; `curl` reconfirmed `/articles/admin` 302s anonymously while `/articles` and `/` still 200.
  - Pushed to production (`7649561`).
- **Orphaned-photo cleanup made uniform via `ISupportImageAttachments`, using an EF Core `SaveChanges` interceptor (2026-09-08)**: the previous entry's "noticed in passing" bug (`ArticleService.DeleteAsync` never cleaned up its photo files) turned out to also affect `StorageService.DeletePropertyAsync` - both were plain `Remove()` + `SaveChanges()`, never touching `IImageService` at all. Rather than patch each one by hand (the same class of gap that would keep recurring at every future call site), the fix is structural: new `Services/ImageCleanupSaveChangesInterceptor.cs` (an EF Core `SaveChangesInterceptor`, registered on `ApplicationDbContext`'s `DbContextOptions`) inspects every `SaveChanges`/`SaveChangesAsync` call for any tracked entity in `EntityState.Deleted` that implements `ISupportImageAttachments`, and deletes that entity's physical image files - regardless of which service or page triggered the delete. Entity-specific `Delete` methods (`ArticleService.DeleteAsync`, `StorageService.DeletePropertyAsync`) needed **no code changes at all**; the interceptor catches their existing plain `Remove()` calls automatically. `MyCollectables.razor`'s `DeleteCollectable` had its own manual per-image `ImageService.DeleteImageAsync` loop removed, now relying on the interceptor too - one implementation instead of duplicated logic that could drift.
  - **How state crosses the two-phase hook**: `SavingChangesAsync` (before the row - and its cascade-deleted `ImageAttachment` rows - are actually removed) walks `ChangeTracker.Entries()`, and for each `Deleted` `ISupportImageAttachments` entity, explicitly loads its `Images` navigation (`entry.Collection("Images").LoadAsync()`) if not already populated, since e.g. `ArticleService.DeleteAsync`'s `FindAsync(id)` doesn't `.Include()` it - the rows would otherwise be gone from the DB by the time cleanup could look them up. The captured list is then deleted for real in `SavedChangesAsync` (after the DB delete has committed) via the ALREADY-injected `IImageService.DeleteImageAsync`. State is passed between the two hooks via a `ConditionalWeakTable<DbContext, List<ImageAttachment>>` keyed on the specific `DbContext` instance, **not an interceptor instance field** - the interceptor itself is a singleton shared by every short-lived `DbContext` the app's `IDbContextFactory` creates, so an instance field would let concurrent `SaveChanges` calls from different circuits corrupt each other's pending-delete list (the exact class of bug this project's earlier `IDbContextFactory` conversion work was all about avoiding). `SaveChangesFailedAsync` clears the table entry on a failed save, for hygiene.
  - **DI lifetime fix needed to wire this in**: `IImageService` was registered `AddScoped`, but `ApplicationDbContext`'s options (and therefore the interceptor passed to `.AddInterceptors(...)`) are configured once, resolving from the app's root provider - which throws "cannot consume scoped service from singleton" if the interceptor depends on a scoped service (the exact DI validation error already documented elsewhere in this file for a similar `AddDbContextFactory`-adjacent mistake). Fixed by promoting `IImageService` to `AddSingleton` - safe, since `ImageService`'s only dependencies are `IWebHostEnvironment`/`ILogger<ImageService>`, both singleton-safe, with no per-request state at all. `ImageCleanupSaveChangesInterceptor` is also registered as a singleton and resolved via the `Action<IServiceProvider, DbContextOptionsBuilder>` overload of `AddDbContextFactory`.
  - **The one gap a `ChangeTracker`-based interceptor genuinely cannot close on its own, and how it's closed anyway**: `Collectable.UserId -> ApplicationUser` is `DeleteBehavior.Cascade`, so deleting a user via `AllUsers.razor`'s `UserManager.DeleteAsync` would cascade-remove their `Collectable`s (and transitively their `ImageAttachment`s) **purely at the SQL level** if those `Collectable`s were never loaded into the same `DbContext`/`ChangeTracker` that runs the delete - completely invisible to any EF-level interceptor, since the tracker never sees rows it never loaded. Confirmed via the schema that this is the *only* such gap for image-owning entities: `StorageProperty`'s and `Article`'s audit FKs (`CreatedByUserId`/`ModifiedByUserId`/`AuthorUserId`) are all `Restrict`, not `Cascade`, so deleting a user never cascades into either of those. Fixed by having `AllUsers.razor`'s `HandleDeleteConfirmed` explicitly load and `RemoveRange` the user's `Collectable`s (and, same as `MyCollectables.razor`, their `CollectableCollectionItem` membership rows first, since that FK is `Restrict`) in their own `IDbContextFactory`-created context and `SaveChangesAsync()` call **before** calling `UserManager.DeleteAsync` - this makes the interceptor see and clean up those `Collectable`s' photos through the exact same uniform path as any other explicit delete, rather than needing any cascade-specific special-casing in the interceptor itself.
  - **Verified end-to-end for all four real scenarios**, using a throwaway console harness (`ProjectReference`-to-the-real-project, deleted after - including a hand-rolled `IWebHostEnvironment` stub and a manually-constructed `UserManager<ApplicationUser>` to create a real throwaway user + collectable + image without going through any HTTP/login flow) to insert real rows with real physical files run through `ImageService.SaveImageFromDiskAsync` (so thumb/medium variants exist exactly like a real upload), then deleting each via the actual app UI (temporary `[Authorize]` removal on `Articles.razor`, `StorageProperties.razor`, `AllUsers.razor`, `MyCollectables.razor`, all reverted immediately after) and confirming via direct disk inspection that every file was gone afterward: (1) Article delete - previously-broken, now correctly cleans up; (2) StorageProperty delete - same, previously-broken, now correct; (3) Collectable delete via `MyCollectables.razor` - regression check on the simplified code, still correct, including its `CollectableCollectionItem` membership-row cleanup; (4) **the cascade case** - deleted a real throwaway user with their own collectable/image/system-collection via `AllUsers.razor`, confirmed the user, collectable, image row, and collection all cascaded away in the DB *and* the physical files were actually removed from disk, proving the `AllUsers.razor` fix correctly feeds the interceptor. No errors in the server log across all four deletions. Test data (DB rows and physical files) fully removed as a side effect of the deletions themselves; `[Authorize]` attributes restored and reconfirmed 302ing anonymously afterward.
  - `dotnet build --no-incremental` clean: 0 errors, 0 warnings.
  - Committed locally; not yet pushed - confirm with the user before pushing.

### Next Steps / In Progress
- RV & Boat Storage: Phase 2 (not started) is customer self-signup with the "Storage Customer" role, and recurring billing (PayPal or similar) wired to `StorageRental` — `BillingFrequency` and `PaymentDate`/`GetNextBillingDate` are meant to set up exactly this, giving Phase 2 a billing cycle and anchor date to key off per rental. Phase 2's signup flow will need to actually assign the Storage Customer role at signup time, now that the rental picker depends on it being assigned already. `Invoice`/`Payment`/`Receipt` entities are deferred until a payment provider is chosen. Once a real public Storage landing page exists, wire `IArticleService.GetFeaturedForStorageLandingAsync` into it (already built, just unused).
- Give the new `/storage/admin` area — and now the whole MudBlazor-9.9.0 UI generally — a manual pass while actually signed in. It's had several individual direct-browser checks (see above, including post-upgrade) but not a full end-to-end pass, since Claude won't type the seeded Admin's password into the login form.
- Articles email-sending was explicitly deferred (see above) — revisit if/when the user wants it.
- Retrofitting existing `int`-keyed entities to `Guid` PKs was explicitly discussed and deferred as its own project, not bundled into the Articles work.
- **MudBlazor upgrade (6.21.0 → 9.9.0) is complete and pushed to production** (see above) — all three version steps browser-verified, `dotnet build` clean.
- The .NET 10 upgrade is pushed and confirmed working in production, including the `UseStaticFiles()` restoration for the image-loading regression (see above) — still waiting on user confirmation that redeploying actually fixed the broken images.
- The 2026-09-07 nav-menu/Home-heading/Articles-visibility/sidebar-drawer/BL0008/article-cropping/MudGrid fixes (see above) are all pushed to production (`16b7fba`, `cc484e5`, `4902de7`, `91d522c`).
- The 2026-09-07 DbContext-concurrency hardening (see above) is pushed to production (`ce97d5a`).
- The 2026-09-08 Article media layout templates (see above) are pushed to production (`7649561`). The drag-and-drop photo reordering in `ArticleDialog.razor` hasn't been manually tested by a real signed-in user yet (see above) - worth a quick pass.

---

## IRC Event Logging API

### Overview
REST API endpoint for logging IRC events from Python bots to the SQL Server database.

### Endpoint
```
POST /api/irc/events
```

### Authentication
The API uses a static API key passed in the `X-Api-Key` header. Valid keys are configured in `appsettings.json` under `IrcApi.ApiKeys`.

### Request Headers
| Header | Required | Description |
|--------|----------|-------------|
| `Content-Type` | Yes | Must be `application/json` |
| `X-Api-Key` | Yes | Valid API key from configuration |

### Request Body
```json
{
  "action": "MESSAGE",
  "network": "Libera.Chat",
  "channel": "#mychannel",
  "target": "targetuser",
  "message": "Hello, world!",
  "sender": "botuser",
  "user": "someuser",
  "timestamp": "2025-10-10T12:00:00Z"
}
```
Note: `timestamp` is optional. If omitted, it defaults to the current UTC time.

### Fields
| Field | Type | Required | Max Length | Description |
|-------|------|----------|------------|-------------|
| `action` | string | Yes | - | Event type (see valid actions below) |
| `network` | string | Yes | 100 | IRC network name |
| `timestamp` | DateTime | No | - | UTC timestamp of the event (defaults to current UTC time if not supplied) |
| `channel` | string | No | 200 | Channel where event occurred |
| `target` | string | No | 200 | Target of the action |
| `message` | string | No | 4000 | Message content |
| `sender` | string | No | 100 | User who triggered the event |
| `user` | string | No | 100 | Additional user context |

### Valid Actions
`JOIN`, `PART`, `MESSAGE`, `QUIT`, `KICK`, `MODE`, `ACTION`, `NOTICE`, `CONNECT`, `TOPIC`

### Response
- **201 Created**: Event logged successfully, returns `{ "id": <int> }`
- **400 Bad Request**: Invalid action value
- **401 Unauthorized**: Missing or invalid API key

### Sample curl Call
```bash
curl -X POST https://localhost:5001/api/irc/events \
  -H "Content-Type: application/json" \
  -H "X-Api-Key: your-api-key-here" \
  -d '{
    "action": "MESSAGE",
    "network": "Libera.Chat",
    "channel": "#mychannel",
    "message": "Hello from the bot!",
    "sender": "mybot"
  }'
```

### Sample Python Call
```python
import requests

response = requests.post(
    "https://your-domain/api/irc/events",
    headers={
        "X-Api-Key": "your-api-key-here",
        "Content-Type": "application/json"
    },
    json={
        "action": "MESSAGE",
        "network": "Libera.Chat",
        "channel": "#mychannel",
        "message": "Hello from the bot!",
        "sender": "mybot"
    }
)

if response.status_code == 201:
    event_id = response.json()["id"]
    print(f"Event logged with ID: {event_id}")
```

### Related Files
- Entity: `PatinaBlazor/Data/IrcEvent.cs`
- Enum: `PatinaBlazor/Data/ChatAction.cs`
- Service: `PatinaBlazor/Services/IrcEventService.cs`
- Endpoint: `PatinaBlazor/Endpoints/IrcEventEndpoints.cs`
- Config: `PatinaBlazor/Services/IrcApiSettings.cs`
